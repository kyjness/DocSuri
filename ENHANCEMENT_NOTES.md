# DocSuri 고도화 & 에이전트 도입 방향 노트

> **이 문서의 위상**
> - 이것은 **작업/의견 노트**다. `aidlc-docs/`의 동결된 AI-DLC 산출물이 *아니다*.
>   여기서 방향을 자유롭게 다듬은 뒤, 합의된 항목만 정식 절차(`handoff.md §6`:
>   프롬프트→계획→승인→산출물 동기)를 거쳐 `aidlc-docs/`에 반영한다.
> - **전제**: 현재 구현(U0~U4, mock/aws)은 *"돌아가는지 본 최소 데모"*다. 정답이 아니라
>   출발선이며, 고도화는 당연히 한다. 데모용 제약(아래 §1)은 *해제 대상*으로 본다.
> - **읽는 사람 기준**: 코드 세부를 몰라도 이해되게 쓴다. 전문 용어는 *괄호로 쉬운 말*을 붙인다.
> - **갱신 방식**: 계속 여기서 자유롭게 수정·추가한다.

---

## 용어 (먼저 읽기)

세 단어가 계속 헷갈리니 먼저 고정한다.

| 용어 | 한 줄 | 축 |
|---|---|---|
| **도구(tool)** | 밖에서 한 번 부르면 답 주는 것 (블랙박스) | 역할 — 남이 *부름* |
| **워크플로(workflow)** | 여러 단계를 *코드가* 정한 순서로 돈다 | 내부 — *코드*가 운전 |
| **에이전트(agent)** | 다음 행동·종료를 *LLM이* 스스로 정한다 | 내부 — *LLM*이 운전 |

- **도구는 다른 축**이다: 워크플로·에이전트 모두 *밖에선 도구로* 호출될 수 있다
  (예: U1 검색 = 내부는 워크플로, 밖에선 "검색해줘" 한 번 호출 = 도구).
- **워크플로 vs 에이전트 = 운전대 누가 쥐냐**: 코드면 워크플로, LLM이면 에이전트.
- **U3** = 코드가 운전하되 딱 한 곳에서만 "어디로?"를 LLM에 묻는 워크플로 = "가드 붙은 워크플로"(에이전트 아님).

---

## 0. 한눈 요약

- **무엇을 만드는가**: 논문을 *찾고(U1) → 이해하고(U2) → 내 주제와 차별화하고(U3) →
  인용을 추적하는(U4)* **연구 보조 도구**. 진짜 목표는 검색기가 아니라 **"연구 파트너"**다.
- **고도화 = 데모가 일부러 닫아둔 것을, 사용자(페르소나)가 실제로 겪는 문제 순서로 다시 여는 것** (§1·§4).
- **자율 에이전트(LLM이 흐름을 운전)는 꼭 필요한 데만** — E5·E6 + 3층. U3는 *가드 붙은 워크플로*,
  U1·U2·U4는 *도구로 호출되는 워크플로*. 전부 품질은 고도화한다 (§2·§3).
- **코드 설계(구조)는 유지**, **실행 환경(Lambda 서버리스)은 교체** (§6·§8).

---

## 1. 큰 전환: "데모 제약"을 해제한다

현재 문서의 여러 제약은 *기능 한계가 아니라 데모 비용·인프라 사정*으로 걸어둔 것이다.
제품으로 가려면 아래를 **의도적으로 해제**한다.

| 데모 제약 | 출처 | 왜 데모용이었나 | 제품에서는 |
|---|---|---|---|
| 월 LLM 비용 $50 한도(넘으면 차단) | NFR-COST-01 | 데모 트래픽 예산 | **제거.** 비용은 "막는 것"이 아니라 *모델 골라쓰기 + 사용자별 한도*로 관리 (§7) |
| 로그인 없이 익명으로만 사용 | NFR-SEC-01 | 인증 만들기 회피 | **계정 도입.** E6 알림·E7 개인화의 전제이자 *에이전트가 기억을 저장할 곳* (§6·§9) |
| 풀 서버리스(Lambda) | ADR-D9 | *"안 쓸 때 새는 고정비가 예산의 적"* = $50 한도 때문 | **재검토.** 에이전트(오래 실행될 수 있고[장시간], 중간 상태를 계속 기억해야 하고[상태], 작업을 동시·순차로 이벤트 기반 처리해야 하므로[비동기])와 안 맞음 (§6) |
| 서버가 아무것도 기억 안 함 | ADR-D9 결과 4 | Lambda가 기억을 못 해서 | **서버 기억 도입** (계정·에이전트 작업 상태) |
| 코퍼스(논문 모음) AI/ML 100편뿐 | A5 | 데모 규모 | **실규모 코퍼스 + 분야 확장 + 성능 측정셋** (§5·§9) |
| 외부 논문 API 2개(arXiv·S2)뿐 | NFR-DATA-01 | 최소 데모 | **여러 소스로 확장** (§5) |
| 4개 기능만(E5·E6·E7 제외) | epics.md | 데모 범위 | **미뤄둔 기능 재개방** (§4.5) |

> ⚠️ 단, "$50 제거"가 "비용을 안 본다"는 뜻은 아니다. 관측·예산은 *오히려 더* 중요해진다
> (에이전트는 한 작업에 AI를 여러 번 부르니까). 성격이 *하드 차단 → 정책/골라쓰기*로 바뀔 뿐.

---

## 2. 핵심 원칙: "고도화"와 "자율성"은 다른 축이다

가장 중요한 구분. **모든 기능은 고도화 대상이다. 그중 일부만 자율 에이전트가 된다.**

- **자율성** = 다음 행동을 *LLM이 스스로* 정하느냐(=에이전트). 코드가 정하면 워크플로.
- **고도화** = 그냥 더 좋게 만드느냐 (품질 향상, 전부 해당)

```
                  자율성 ↑  (LLM이 흐름을 운전)
                        ▲
              E5 재현성 ◌                     (잠정: 자율 에이전트 · 판정 미정)
                        │
              U3 차별화 ●                     (워크플로 + 판단 1곳) ✅
                        │
   ─────────────────────┼─────────────────────▶  품질 고도화 (더 좋게)
                        │
   U1 검색 ●  U2 요약 ●  U4 인용 ●  E6 트렌드 ◌   (워크플로 — 코드가 운전)
```

- 가로축(고도화)은 **전부 오른쪽으로**. U1·U2·U4도 다단계 *워크플로*로 제대로 만든다(자판기 단발 아님).
- 세로축(자율성)은 **필요한 것만** 위로. ● = 확정/제안 · ◌ = 내부 형태 *판정 미정*(§11.A).

> "검색·요약을 *자율 에이전트*로 만들지 말자"(세로 낮게)와 "고도화하자"(가로 높게)는 **동시에 참**.
> ⚠️ **E5·E6는 아직 "에이전트로 추천"이 아니다** — 잠정 짐작(E5=에이전트 쪽, E6=워크플로+스케줄러 쪽)일 뿐,
> U3처럼 4기준 판정을 거쳐야 확정(§11.A·§4.5). **E7 개인화는 2층 타일이 아니라 횡단 능력**(계정·상태)이라 이 그림에 없음(§9).

---

## 3. 목표 아키텍처: 3층

> **먼저 용어 두 축을 분리** (이게 계속 헷갈렸던 지점):
> - **내부 구조 (어떻게 동작?)**: 단일 호출 → **워크플로**(코드가 여러 단계 운전) → **에이전트**(LLM이 운전)
> - **역할/계층 (시스템 어디?)**: **도구**(남이 부르는 블랙박스) ↔ **오케스트레이터**(남을 부르는 것)
>
> **두 축은 직교다.** 예: U1 검색은 *내부는 워크플로*(여러 단계)지만 *밖에선 도구로 한 번* 호출된다.
> 그래서 "도구냐 워크플로냐"는 양자택일이 아니라 *보는 관점* 차이.

층은 *내부 구조가 아니라* **"조합 범위"로** 나눈다 — 단위 능력 → 과업 → 목표:

```
[3층] 오케스트레이터  (LLM supervisor = 자율 에이전트)        ⏳ 미룸
       열린 목표 "내 졸업주제 X 알아서 도와줘" → 1·2층을 도구로 조합
        │
[2층] 작업 계층  (사용자 과업 하나를 완결)
       U3 차별화 · E5 재현성 · E6 트렌드   — 내부는 과업마다 다름
       (U3 = 워크플로 ✅ · E5·E6 = 판정 미정 — 잠정 E5=에이전트, E6=워크플로+스케줄러)
       ※ E7 개인화는 2층 타일이 아니라 횡단 능력(계정·상태, §9)
        │
[1층] 도구 계층  (재사용 단위 능력 · 위층이 부품으로 호출)
       U1 검색 · U2 요약/번역 · U4 인용   — 내부는 대부분 워크플로(LLM은 필요한 단계만)
```

- **1층 = 도구 계층**: 검색·요약 같은 *단위 능력*. *내부는 워크플로*(U1·U2·U4 다단계)지만 위층은 한 번 호출. AI 출력은 JSON 계약으로 고정해 위층이 깨지지 않게 부른다 (§9).
- **2층 = 작업 계층**: 1층을 엮어 *사용자 과업*을 완결(예: 차별화 리포트). 내부 구조는 과업마다 판정 — **다 에이전트가 아니다**(U3는 워크플로).
- **3층 = 오케스트레이터**: 열린 목표를 받아 1·2층을 도구로 조합. *이 층만 LLM이 운전 = 진짜 자율 에이전트(supervisor/sub-agent)*. 가장 큰 베팅이라 미룸(§11.A).

**왜 이 구조인가**:
- 층을 *조합 범위*로 쌓으면 아래가 위의 부품이 됨 → 재사용·교체·테스트 쉬움 (1층 도구를 2·3층이 공유).
- 내부 구조(워크플로/에이전트)는 기능마다 *필요한 만큼만* → 비용·예측가능성 통제 ("start simple").
- 자율(LLM 운전)은 위험·비용이 커서 *맨 위 한 곳(3층)에만*, 그것도 1·2층 안정 후.
- 1층은 동결 AI-DLC unit(U1·U2·U4), 2층은 U3 + 재개방 E유닛에 매핑 → 기존 문서와 추적성 유지.

### 3.1 잠정 전체 지도 (확정/제안/팀대기 구분)

> ⚠️ 잠정. 2층 타일 선정(11.B)·동결 재개방(11.C)이 정해져야 *전체*가 확정된다.
> 범례: **✅ 확정된 결정** · **⏳ 내 제안(미확정)** · **❓ 팀/타주체 결정 대기(또는 동결 재개방 필요)**

```
                               사용자
                                 │
 ┌───────────────────────────────────────────────────────────────┐
 │ [3층] 오케스트레이터 = LLM supervisor(자율 에이전트) · ⏳ 타이밍: U3 후  │
 │   "주제 X → 공백+선행연구 정리+새 논문 알림"  1·2층을 도구로 조합     │
 └───────────────┬───────────────────────────────────────────────┘
                 │ (나중)
 ┌───────────────┴───────────────────────────────────────────────┐
 │ [2층] 작업 계층 (과업 완결 · 내부는 과업마다 워크플로/에이전트)         │
 │   ✅ U3 차별화 = 가드 워크플로 (프레임 B, §4.3.1)                    │
 │   ❓ E5 재현성  = 잠정 에이전트(판정 미정) · 재개방(준희님) 선행          │
 │   ❓ E6 트렌드  = 잠정 워크플로+스케줄러(판정 미정) · 재개방 선행          │
 └───────────────┬───────────────────────────────────────────────┘
                 │ '도구'로 호출
 ┌───────────────┴───────────────────────────────────────────────┐
 │ [1층] 도구 계층 (단위 능력 · 내부는 대부분 워크플로 · 출력 JSON 계약)    │
 │   ⏳ U1 검색 = production 파이프라인 워크플로 (§4.1)                  │
 │   ⏳ U2 요약/번역 = 도구 + 충실도·청킹 고도화 (§4.2)                  │
 │   ⏳ U4 인용 = 도구 (현 구현 유지 + 소폭, §4.4)                      │
 └───────────────────────────────────────────────────────────────┘

 횡단(전 층 공통):
   ⏳ 충실도/근거 강제(새 NFR)   ❓ eval 골든셋(팀)   ❓ 작업예산 수치(팀)
   ❓ 계정/상태(팀·동결 NFR-SEC-01)   ❓ 실행환경: 상시컨테이너+큐+스케줄러(팀)
   ❓ E7 개인화 = 횡단 능력(계정·상태 위에 얹힘 · 2층 타일 아님)
```

읽는 법: **✅는 닫힌 결정**(U3 프레임·3층 타이밍), **⏳는 내가 민 방향**(U1·U2·U4·충실도),
**❓는 나 혼자 못 정하는 것**(E5·E6 재개방, 인프라, 비용수치, 계정, eval). 회의에서 ❓를 닫으면 이 지도가 실선으로 굳는다.

---

## 4. 영역별 고도화 방향

### 4.1 U1 검색 — production 검색 파이프라인 (워크플로)

> 현재 실서비스 검색의 표준 레시피 = **넓게 검색 → 융합 → dedup → 리랭킹**. 에이전트가 아니라
> *워크플로*고, LLM은 1번만(1단계) 쓴다. 표기: **[코드]** AI 안 부름 · **[도구]** AI 단일 호출 ·
> **[모델]** 전용 모델(임베딩·리랭커 — LLM 아님).

```
0. [코드]  입력 무해화 (현 U1 safety 재사용)
1. [도구]  쿼리 이해 — 한→영 매핑 + 동의어·약어 확장 (LLM 1콜, JSON 출력, 캐시)
           · recall↑. 단 확장은 노이즈도 늘리므로 5단계 리랭킹과 짝.
2. [코드]  후보 생성 (넓게·병렬, 수백 건):
           · [모델] 밀집 = 임베딩 ANN          (의미 유사)
           ·        희소 = BM25/키워드          (정확 단어·고유명사·약어)
           ·        멀티소스 = arXiv·OpenAlex·S2…  소스별 독립 호출 + 서킷브레이커
3. [코드]  융합 — RRF로 여러 랭킹(밀집+희소+소스별) 합치기  (파라미터 적고 robust)
4. [코드]  dedup — DOI 정확매칭 + 제목 퍼지매칭으로 중복 제거  (멀티소스면 필수)
5. [모델]  리랭킹 — cross-encoder로 상위 50~100 정밀 재정렬 → top-k  (정확도 핵심 레버)
6. [코드]  난이도 추정·정렬·필터 (U1 기존) → SearchResult
   캐시: 쿼리 정규화 키로 결과 캐시 (현 24h 유지)
```

- **성능**: 비싼 건 LLM뿐인데 **1번(확장)만** + 캐시. 리랭킹은 *전용 리랭커 모델*이라 50건 수십~수백 ms.
  → NFR-PERF(검색 P50<3s) 안에 들어옴.
- **출력 형식 고정**: 1단계 확장·매핑을 JSON 계약으로 (현 `KO_EN_SEED` 수동사전 + `_parse_terms` regex 취약점 제거).
- **코퍼스**: 100편 시드 → 실규모 + 분야 태그·인용수 실데이터 (현재 일부 임시값).
- **왜 이게 표준/최선인가**: 위 6단계가 곧 "하이브리드 + 멀티소스 + 리랭킹"의 실제 구현이고 현 업계 합의안.
  더 단순(벡터만)하면 정확도↓, 더 복잡(자율 에이전트)하면 비용·예측불가↑. DocSuri 규모에 right-sized.
- **맥락 전제**: "최선"은 *학술 코퍼스·중간 트래픽* 가정. 초대규모/초저지연이면 인덱스·샤딩·캐시 계층을 더 봐야 함.

> 결론: U1은 **에이전트가 아니라 "검색 엔진 자체를 제대로"** 만드는 워크플로 고도화.

### 4.2 U2 이해(요약·번역) — production 파이프라인 (워크플로)

> 핵심 = **믿을 수 있는 요약**: 섹션 인지 청킹 → 근거 고정 생성 → 충실도 검증. 에이전트가 아니라
> 워크플로. 표기: **[코드]** · **[도구]** AI 단일 호출 · **[모델]** 전용 모델(NLI·멀티모달 — LLM과 별개일 수 있음).

```
입력: PDF / arXiv URL / 본문텍스트 + persona(전문/학부)
0. [코드]  수집 — PDF→텍스트(레이아웃 인지), arXiv API, 섹션 분할(제목·초록·본문·캡션)
1. [코드]  섹션 인지 청킹 — 의미 단위 + 오버랩  (현 "머리+꼬리 자르기" 폐기)
2. [코드]  용어 사전 적중 — 청크 내 학술어 추출 (U0 Glossary)
3. [도구]  요약 생성 — 맵리듀스(청크별 부분요약→종합) 또는 long-context 1콜
           · persona 분기: 전문=어휘 보존 / 학부=풀어쓰기·약어·수식 해석
           · 출력 JSON 계약: {question, method, result, limit}  (현 regex 파싱 폐기)
           · grounding: "제공 텍스트 안에서만 + 각 항목에 근거 위치(페이지/span)"
4. [모델]  충실도 검증 — 각 요약 문장이 원문 근거와 맞나 (NLI 모델 또는 LLM-judge)
           · 미달 문장 → 1회 재생성
5. [코드]  가독성 검증 — 현 ReadabilityValidator 확장: 학부=문장길이·어휘 / 전문=용어 보존
           · 미달 → 재생성 (현재 학부만 → 전문도)
6. [코드]  조립 → 캐시(7d) → Telemetry(토큰·비용·persona)

곁가지(같은 뼈대):
- 번역(부분): 선택 단락 → Glossary 용어 고정 + LLM 1콜 → JSON {target, glossary_hits}
- 그림 설명: 캡션+주변 문맥 → 1~2문장. ❓ 표·그림 *자체*를 보려면 멀티모달(이미지 입력) 필요
```

- **성능** (NFR-PERF-02 요약 P95<20s): 맵리듀스 청크는 병렬, 충실도 검증은 *빠른 NLI 모델* 권장, 캐시 7d로 재요약 회피.
- **충실도가 U2의 핵심**: 연구 도구에서 환각은 치명 → "근거 밖 생성 금지 + 문장별 근거" + 4단계 검증으로 NFR-DATA-02를 실제로 강제.
- **❓ 팀 입력 필요**:
  - 충실도 *합격 기준치*(어디까지 통과로 볼지) → **준희님**(제품 품질 바)
  - 표·그림 멀티모달(이미지 보는 모델·비용) → **석현님**(아키텍처)·**화랑님**(비용)
  - PDF 레이아웃 추출 라이브러리·비용 → **석현님**

> 결론: U2도 **에이전트가 아니라 "요약을 믿을 수 있게"** 만드는 워크플로 고도화.

### 4.3 U3 차별화 — 2층 첫 타일 (착수 1순위)

| 근거 | 내용 |
|---|---|
| **본질이 다단계** | 주제초안→유사논문 5편 검색→논문별 비교노트→전반 novelty 종합→연구공백 후보+근거. 한 번 물어봐선 안 됨. (설계 문서 component-model §5가 이미 단계별로 쪼개놨음) |
| **빠를 필요가 없음** | U1/U2/U4는 "몇 초 안에" 같은 속도 목표에 묶여 있다. 그런데 **U3엔 속도 목표가 없다**(US-DIFF-01에 응답시간 기준이 없음) → 느린 다단계 에이전트가 기존 요구사항을 안 깨는 *유일한 자리* |
| **아직 안 만들어짐** | 백엔드에 U3 코드가 없다. 깨뜨릴 게 없는 백지 |
| **비용이 많이 드는 자리로 이미 지목됨** | 위험 R3 / 설계 §8.3. "한 작업당 예산"이 필요한 첫 사례 (§9) |

> **결정됨 (2026-06-14)**: **가드 붙은 워크플로** — 고정 뼈대 + 제한적 판단 1~2곳 + 근거 강제.
> 완전 자율 에이전트는 제외(novelty·공백은 자동 검증 불가 + 사용자가 신뢰 → 통제 필요). 상세 §11.

#### 4.3.1 U3 워크플로 v1

> 이건 현 AI-DLC의 *임시* U3 설계(component-model §5 고정 파이프라인)를 그대로 쓰는 게 아니라,
> 고도화·재구조한 제안이다 (구조화 출력·근거 게이트·작업 예산·U0 포트 기반 검색이 새로 들어감).
> 표기: **[코드]** AI 안 부름 · **[도구]** AI 단일 호출 · **[판단]** 유일한 가드 · **[검증]** 지어내기 차단.

```
입력: 주제 초안(200~500자) + persona(전문/학부)
0. [코드]   입력 정규화·무해화 (길이·언어 검증 + 인젝션 가드 — U1 safety 재사용)
1. [코드]   초안 임베딩 → 넓게 검색(후보 30~50) → 리랭킹 → 깊은 비교 대상 top N(5~8)  (안 B)
2. [판단]   ★유일한 에이전트적 결정★ "이 N편이 초안과 관련 있나?"
            적절→다음 / 부적절→검색어 다듬어 1회만 재검색 (하드캡=1)
            출력 고정: {adequate, refined_query?}
3. [도구×N] 논문별 비교노트 "공통점 1줄 / 차이점 1줄"  (N≈5~8)
            입력: 초안 + 논문메타 (+있으면 U2 요약) · 출력 고정 {common,diff} · 근거: 그 논문 1편
4. [도구×1] 전반 novelty 한 문단 (입력: 노트들만 / "노트 밖 내용 생성 금지")
5. [도구×1] 연구공백 후보 3개 (DIFF-02) — 방법·데이터셋·평가지표 차이
            출력 고정: [{title, body, evidence_ids}]×3
6. [검증]   근거 게이트: 모든 evidence_ids ∈ 검색된 후보 ID(N편)? 위반 후보 폐기(1회 재생성)
7. [코드]   NoveltyReport 조립 → 캐시 → Telemetry(작업당 콜수·토큰·비용)

[작업 예산] 래퍼가 전체를 감쌈 — 한 요청의 LLM 콜/토큰 상한 (R3 = 비용 폭증 자리, 첫 적용처)
```

- 결정 매핑: 고정 뼈대 = 0·1·3·4·5·7 / 판단 1곳 = 2 / 근거 강제 = 3·6 / 자율 제외 = 그런 칸 없음
- 비용 감각: 노트 N(5~8) + 종합 1 + 공백 1 + 판단 1 (재검색 시 +N) = 한 요청 **약 8~18콜** (리랭킹은 별도 모델 — LLM 콜 아님)
- 학부(DIFF-03, Could): 0·1·2 뼈대 공유, 3~5를 "가벼운 점검 1콜"로 축소 (별도 에이전트 아님)
- 입력 의존: U1 `SearchResult`(유사 논문 메타) · U2 `SummaryResult`(선택, 노트 풍부화) — 둘 다 mock 가능
- **미정(품질 바)**: novelty·공백의 품질 기준·평가 임계값은 §9 골든셋에서 — *이 워크플로 구조와는 독립*

#### 4.3.2 U3 범위·프레임 대안 (트레이드오프)

> §4.3 결정은 "현재 범위 전제"의 결정이다. 범위·프레임 자체도 선택지가 있고, 이건 *사용자 선택*이다.
> 가정이 아니라 *뒤집을 수 있는 결정*으로 남긴다. (★ = 채택)

| 안 | 무엇 | 장점 | 단점 |
|---|---|---|---|
| A | 고정 top-5 비교 (현 AC 그대로) | 단순·싸·빠름·예측가능 | 가장 가까운 선행연구 놓칠 위험(recall↓) → novelty 신뢰도↓ |
| **B ★** | 넓게 검색(30~50) + 리랭킹 + 깊은 비교 top 5~8 | recall↑인데 깊은 비교는 좁게(비용 통제). 틀 유지 | 리랭커 복잡도. 여전히 임베딩 유사도 프레임 |
| C | B + 인용 그래프 신호 결합 (U4 시너지) | 학술적 novelty 본질에 부합("계보상 빈자리"). U4 데이터 재활용 | 구현 복잡·인용 데이터 품질 의존 → 다음 단계 |
| D | 자유 탐색 에이전트 (문헌 전체를 알아서 파는 연구 파트너) | 가장 깊고 강력, 사람 연구자에 근접 | 비용·지연 폭발 + 검증 불가 주장 위험 + 예측 불가 → **3층 오케스트레이터의 몫, 가장 나중** |

> **결정됨 (2026-06-14): B 채택.** 틀(retrieve→compare→synthesize) 유지, 안에서 고도화.
> 경로: 1차 **B** → U4 성숙 후 **C** → eval·근거 장치 갖춘 뒤 **D**를 3층으로 분리.
> ("그냥 20편 비교"는 깊은 비교를 20개로 늘리는 게 아니라[비용] *검색을 넓히고 리랭킹*하는 B로 흡수.)

### 4.4 U4 인용 — production 파이프라인 (도구, LLM 없음)

> U4는 *데이터 조회*라 LLM을 안 쓴다(가장 쌈). 핵심 = 다중소스 + dedup + 실패/0건 구분.
> 표기: **[코드]** · **[외부]** 외부 API 호출.

```
입력: paper_id (U1 SearchResult.papers[i].id)
0. [코드]  캐시 확인 (cite:{id}, 24h) — 적중 시 바로 반환
1. [외부]  다중소스 인용 조회 (병렬): Semantic Scholar + OpenAlex (+Crossref)
           · 소스별 서킷브레이커 + 타임아웃 + 지수 백오프
           · 429/실패 vs 진짜 인용 0건 구분 → degraded 플래그 (U0-M2·U4-M2)
2. [코드]  병합·dedup — DOI 정확 + 제목 퍼지로 outgoing(인용)·incoming(피인용) 합치고 중복 제거
3. [코드]  절단·정렬 — 그래프 ≤30노드(인용수 가중) / 학부 Top-3(피인용 가중)
4. [코드]  FormFactorRouter — 렌더 모드 결정 (graph: 데스크톱+전문 / 그 외: list)
5. [코드]  캐시 set(24h) → Telemetry
(선택) N-hop 추적 — "인용의 인용"까지. 어디까지 팔지 판단 = 에이전트 → 3층/후속
```

- **성능** (NFR-PERF-03 1-hop P95<5s): LLM 없어 병목은 외부 API뿐 → 캐시(24h)+병렬+서킷브레이커로 흡수.
- **신뢰성**: degraded 플래그로 "조회 실패"와 "진짜 0건"을 화면이 구분 (빈 상태 문구 분기).
- **❓ 팀 입력**:
  - 다중소스 채택·쿼터(아키텍처·데이터원) → **석현님**
  - N-hop 추적(범위·비용) → 후속, 에이전트라 유진님 설계 + 준희님 우선순위

> **개인 제안 (미확정 · 변경절차 필요)** — 작성자 의견
> 현재 렌더 분기(`FormFactorRouter`): **그래프 = 데스크톱(≥768px) + 전문모드일 때만**, 그 외(모바일 전체 / 학부 모드) = 리스트.
> **제안: 모바일에서도 전문 모드는 그래프로 노출하고 싶다.**
> ⚠️ 동결된 NFR-MOBILE-05(<768px는 리스트)와 충돌 → handoff §6 변경절차 필요 (담당: 준희님).
> 트레이드오프: (찬) 전문 사용자에게 디바이스 무관 일관 경험 / (반) 작은 화면에서 ≤30노드 그래프 가독성·터치 타깃·4G 렌더 성능. 절충안 후보: 모바일 그래프는 노드 수 더 줄이거나 핀치줌·미니맵 제공.

### 4.5 미뤄둔 기능 재개방

데모에서 잘린 기능 3개는 *비용·인프라* 때문에 미룬 것일 뿐, 페르소나가 실제로 원하는 기능이다.

| 기능 | 내용 | 관련 페르소나 (문서 근거) | 내부 형태 (잠정 — 판정은 §11.A) |
|---|---|---|---|
| **E5 재현성** | 논문의 코드·데이터 링크를 모아 "실제로 돌아가는지" 검증 | P1 박지훈: *"찾은 논문조차 재현이 어렵다"* + *"재현 가능한 논문 우선 발견"* | **잠정 에이전트** — 코드 받아 *열린* 실행·디버깅이라 미리 다 명세 불가 |
| **E6 트렌드/알림** | 새 논문 알림·분야 트렌드 요약 | P1: *"트렌드를 한눈에"* | **잠정 워크플로+스케줄러** — 모아→묶고→요약 = 예측가능 파이프라인(자율 아님) |
| **E7 개인화** | 관심분야 학습·재방문 시 이어보기 | 양쪽 페르소나의 학습/큐레이션 | **2층 타일 아님** — 횡단 능력(계정·상태, §9) |

> ⚠️ 위 "내부 형태"는 *잠정 짐작*이지 추천·확정이 아니다. 기능별 워크플로/에이전트 **판정은 §11.A에서 U3처럼 4기준**(복잡도·가치·실현성·오류복구)으로 한다. E5·E6 착수 자체도 재개방(준희님) 선행.

---

## 5. 데이터·외부 API 확장

**지금 외부 논문 API는 2개뿐 — arXiv(본문 가져오기) + Semantic Scholar(인용 정보). 늘려야 한다.**

| 소스 | 무엇에 쓰나 | 이유 |
|---|---|---|
| **OpenAlex** | 논문 메타·인용·분야 | 무료·대규모. S2 한 곳 의존 위험(R4)을 분산 |
| **Crossref** | DOI·서지 정보 | 정규 서지와 출처 신뢰도 |
| **Papers with Code** | 코드·벤치마크 링크 | **E5 재현성에 꼭 필요한 데이터원** |
| **Unpaywall / CORE** | 무료 공개 원문 | 본문 확보율↑ (지금은 arXiv만) |
| (분야 넓힐 때) **PubMed·bioRxiv** 등 | 다른 분야 논문 | "AI/ML만" 제한(A5)을 풀 때 |

> 결론: API 2개는 *데모 최소판*이었다. 제품에선 **여러 소스 + 한 곳 죽어도 대체(폴백)**가 맞고,
> 특히 **E5를 하려면 Papers with Code류가 필수**다. (포트/어댑터 구조라 소스 추가가 쉬움 — §8)

---

## 6. 실행 환경 전환 (Lambda → ?)

**"코드 설계"와 "실행 환경"은 다른 것.** 설계는 유지(§8), 실행 환경은 교체.

**왜 Lambda는 데모용이었나** (ADR-D9): *"안 쓸 때 새는 고정비가 예산의 적"*(=$50 한도), 띄엄띄엄
오는 트래픽, 운영 인력 없음. → 전부 데모 전제. 해제하면 근거가 사라진다.

**왜 Lambda가 에이전트와 안 맞나** (Lambda = "요청 오면 잠깐 깨어나 처리하고 다시 잠드는" 방식):

| 충돌 | 내용 |
|---|---|
| **15분 실행 한도** | Lambda는 한 번에 최대 15분만 돌고 강제로 끝난다. 인턴(에이전트)이 여러 단계를 밟으면 길어지고, 특히 E5 재현성(논문 코드를 내려받아 실제로 돌려봄)은 15분을 우습게 넘긴다 → 아예 불가능 |
| **기억을 못 함 (무상태)** | Lambda는 요청 하나를 처리하고 나면 **머릿속을 싹 비운다.** 그래서 "기억해야 하는 것"은 전부 바깥에 떠넘겨야 한다 — 지금도 ① 요약 섹션 접기 상태는 *브라우저*가 기억하게 했고, ② 누적 비용은 *데이터베이스(DynamoDB)*에 적어둔다. 에이전트가 작업 중 끄적이는 메모(중간 추론)도 둘 데가 없어서 매 단계 DB에 적었다 읽었다 해야 하는데, 느리고 번거롭다 |
| **"기다리는" 방식 (동기 요청)** | 지금은 "요청 보내면 → 답 줄 때까지 화면이 붙잡고 기다림"이다 (게다가 중간 관문이 29초 넘으면 끊는다). 인턴 작업은 오래 걸리니 "맡겨두고 → 끝나면 알려줌" 방식이라야 하는데, 결이 정반대다 |
| **요청이 있어야만 돈다** | Lambda는 누가 요청해야 깨어난다. E6 알림·트렌드는 *아무도 요청 안 해도 알아서 주기적으로 도는* 일이라, 정해진 시각에 깨우는 별도 장치(스케줄러)가 필요하다 |

**전환 방향 (후보 — §11에서 확정):**
- **항상 켜져 있는 서버(컨테이너)** — Lambda처럼 잠들지 않고 상시 떠 있어 긴 작업·기억이 가능
  (예: ECS/Fargate, App Runner). *ADR-D1이 이미 "같은 FastAPI 코드로 전환 가능"이라 적어둠* → 이사비용 낮음
- **오래 걸리는 작업용 "대기열 + 일꾼"** — 작업을 대기열에 맡겨두면 일꾼이 꺼내 처리하고 끝나면
  알려주는 구조 (사용자가 화면 붙잡고 안 기다려도 됨)
- **진짜 기억 저장소** — 계정 정보·에이전트 작업 상태를 보관
- **스케줄러** — 정해진 시각에 자동으로 깨우는 장치 (E6 알림/트렌드용)

---

## 7. 비용 관리 방식 전환

| 지금 | 제품 |
|---|---|
| 월 $50 넘으면 무조건 차단 (AI 부를 때마다 미리 검사) | **차단 제거.** 대신: |
| | ① **모델 골라쓰기** — 쉬운 단발 작업엔 싸고 빠른 모델(Haiku), 어려운 추론엔 똑똑한 모델(Opus 등) (ADR-D4가 "후속 허용"으로 이미 열어둠) |
| | ② **사용자별/작업별 한도** (계정이 있어야 가능) |
| | ③ **한 작업당 예산** — 에이전트는 한 작업에 AI를 여러 번 부르므로, "한 번 부를 때마다"가 아니라 "한 작업당 얼마"라는 개념이 새로 필요 |
| | ④ 관측은 *강화* — 작업별로 토큰·비용·단계 수를 기록 |

---

## 8. 유지할 것 (그리고 왜)

| 유지 항목 | 이유 |
|---|---|
| **포트/어댑터 구조** (U0) | "기능은 그대로 두고 속 구현만 갈아끼울 수 있게" 만든 구조. 실행환경 이사·API 추가·가짜↔진짜 모델 전환을 싸게 만들어둔 **최고의 자산** |
| **기능별로 깔끔히 나뉜 모듈 구조** | 기능 간 주고받는 데이터 약속과 의존 방향이 깔끔(빙빙 도는 의존 없음). 에이전트도 이 경계를 그대로 도구로 호출 |
| **관측(Telemetry) 골격** | 에이전트에선 오히려 더 중요. 그대로 확장 |
| **Bedrock/임베딩/벡터 인프라** | 실행환경과 별개. 대체로 유지 (단 임베딩 품질은 실측으로 재검증 — §9) |

---

## 9. 횡단 구멍 (문서에도 없어서 새로 정의해야 함)

| 구멍 | 왜 중요 | 방향 |
|---|---|---|
| **지어내기 방지 (충실도)** | AI가 논문에 없는 내용을 지어내면 연구 도구로선 치명적인데, 지금 이걸 막는 규칙·장치가 하나도 없다 | "근거 없는 내용 금지" 규칙(NFR) + 답의 근거가 원문 어디인지 표시 + 사후 검증 |
| **성능 측정셋 (골든셋)** | "상위 5건 중 관련 ≥3건" 같은 목표를 정해놨지만 *그걸 측정할 방법 자체가 없다.* 측정 없이는 고도화가 됐는지조차 모른다 | 정답이 표시된 평가용 데이터셋 + 자동 채점 |
| **한 작업당 비용 예산** | 지금 비용 검사는 "AI 1번 부를 때마다"다. 에이전트는 한 작업에 AI를 여러 번 부르므로 "한 작업당 얼마" 개념이 필요 | 비용 관문에 "작업 예산" 추가 |
| **상태/계정** | 에이전트의 기억, E6 알림, E7 개인화가 전부 "사용자가 누구인지·이전에 뭘 했는지"를 알아야 가능 | 익명 세션 → 계정 도입 (NFR-SEC-01 개정) |
| **AI 출력 형식을 약속으로** | 코드끼리 주고받는 데이터 형식은 엄격히 약속돼 있는데, *AI가 내놓는 답의 형식은 약속이 없어* 각 기능이 알아서 글자 패턴으로 긁는다(잘 깨짐). 위층 에이전트가 1층 도구를 안정적으로 부르려면 이게 먼저 해결돼야 함 | AI가 정해진 형식(JSON 등)으로 답하도록 약속을 기능 계약으로 격상 |

### 9.1 배포 하드닝 (production 차단선 — 지금 비어 있음)

> "워크플로 설계는 production 모양"이지만, *배포 수준*이 되려면 아래가 채워져야 한다. 전부 새 주제.

| 구멍 | 왜 중요 (왜 배포 차단인가) | 방향 · 담당 |
|---|---|---|
| **에이전트·도구 보안** | E5는 *임의 코드 실행* → 강한 샌드박스 격리 없으면 위험 / 에이전트가 URL·외부 fetch 시 SSRF / 외부 논문 텍스트가 LLM에 되먹임될 때 *간접 프롬프트 주입* / 도구 호출 인가. (CLAUDE.md 보안 표 미반영) | 샌드박스(Managed Agents 컨테이너)·egress 화이트리스트·도구 출력 무해화·도구별 권한 — **석현님·유진님** |
| **실패·부분결과 처리** | 멀티콜 워크플로 중간 실패(노트 N중 일부만 성공) 시 동작 미정 → 깨진 리포트 위험 | graceful degradation(부분 결과+표시)·재시도·멱등 — **유진님** |
| **U3 지연 상한 + async UX** | "perf NFR 없음"이 *무한정 대기*는 아님. 사용자 인내 한계 필요 | 목표 시간 + 초과 시 "맡기고 알림"(비동기) — 새 NFR, **준희님(목표)·석현님(비동기 인프라)** |
| **배포 전 품질 게이트** | 골든셋만으론 부족 — "임계값 미달이면 배포 금지"가 production 조건 | eval 임계값 게이트(CI) — **유진님** |
| **에이전트 트레이싱** | §11.C 로그는 "무엇 수집"만 — 단계별 span·도구호출·판단 트레이스 없으면 prod 디버깅 불가 | 작업 단위 trace(단계·도구·토큰·결정) 설계 — **화랑님·유진님** |
| **프롬프트 버전 관리** | 프롬프트는 사실상 코드인데 버저닝·롤백·회귀 테스트 없음 → 조용한 품질 저하 | 프롬프트 버전·롤백·회귀 eval — **유진님** |

---

## 10. 이걸 AI-DLC 문서로 어떻게 박는가 (내 역할의 실체)

이 프로젝트는 *문서 → 코드 도출* 방식이라(`handoff.md §6`), 에이전트 도입도 **코드 전에 문서로**
들어간다. 이 노트에서 합의된 것을 아래 정식 산출물로 옮긴다:

| 산출물 | 무엇을 |
|---|---|
| **기능(에픽) 재개방** | E5·E6·E7을 "이번엔 안 함"에서 "다음 사이클"로 승격 (§4.5 근거) |
| **새 ADR(결정 기록)** | (a) 에이전트 만드는 방식 (b) 실행환경 전환 (§6) (c) 비용 관리 전환 (§7) (d) 로그인/계정 |
| **설계(component-model) 개정** | U3를 에이전트로(§4.3) + 2·3층 컴포넌트 추가 |
| **새 NFR(품질 기준)** | 충실도, 작업 예산, 에이전트 작업의 응답 목표, 성능 측정 지표 |
| **새 기능 단위(unit)** | 오케스트레이터(3층)·성능 평가 파이프라인 등 |

---

## 11. 미결 결정 — 누가 정하나

> **팀 역할**: **준희님(PM)** 개인화·차별화 경험·체류 · **석현님(Architect)** 아키텍처 디벨롭 ·
> **유진님(Agent 개발 = 나)** agent 도입 부분 정의 · **화랑님(기획)** 로그 수집·수익화.
> 이 노트 전체가 *팀에 들고 갈 제안*이고, "결정"도 정식 반영(handoff §6) 전까진 제안이다.
> 동결 항목(NFR-SEC-01·A5·E5~E7 등) 변경은 handoff §6 4단계를 반드시 거친다.

### 11.A [유진님 / 나] — 내가 정함 (agent 도입 정의 = 내 영역)

- [x] **U3 워크플로·프레임(B)** — 완료. §4.3 / §4.3.1 / §4.3.2
- [x] **3층 오케스트레이터 타이밍** → 결정(2026-06-14): 2층(U3) 검증 후로 미룸 (start simple)
- [ ] **각 2층 기능의 워크플로 vs 에이전트 판정** — U3 방식대로 기능마다 1회 *(대상·순서는 11.B 뒤)*
- [ ] **U1~U4 production-급 설계 작성** — 기존 unit은 지금 착수 가능 (안의 ❓만 담당자에게)

### 11.B [유진님 결정 + 타 역할 입력] — 입력 듣고 최종은 내가 (회의 가서 정해 와야 할 것)

- [ ] **에이전트 구현 방식** (직접 vs 프레임워크) — **유진님 결정** + **석현님(아키텍처)** 입력.

  | 방식 | 장점 | 단점 | 언제 |
  |---|---|---|---|
  | **직접** (Claude API + tool use) | 내부 투명·의존 최소·완전 유연 | 루프·상태·재시도 직접 구현 | 워크플로/가벼운 에이전트(U3류). **현 추천 출발점** |
  | **프레임워크** (LangGraph 등) | 그래프·멀티에이전트 오케스트레이션 기성품·시각화 | 추상화가 내부 가림·생태계 종속·디버깅 난이 | 복잡한 멀티에이전트(3층) 갈 때 |
  | **Managed Agents** (Anthropic 호스팅) | 에이전트 루프 + *샌드박스 컨테이너* 호스팅 | Anthropic 종속·세션 비용·세부 제어↓ | E5 재현성처럼 *코드 실행 샌드박스* 필요할 때 |

  > 잠정: 직접부터 → 3층/E5 가며 프레임워크·Managed Agents 검토. 결정 시 최신 Claude 모델·SDK 별도 조사.

- [ ] **2층 다음 타일 우선순위** (E5/E6/E7) — **유진님 순서 제안** + **준희님(PM)** 우선순위 입력.

  | 후보 | 페르소나 가치 | 난이도·의존 | 비고 |
  |---|---|---|---|
  | E5 재현성 | P1 핵심 아픔("재현 어렵다")·강한 차별화 | 高 — 샌드박스 + Papers with Code | Managed Agents와 시너지 |
  | E6 트렌드/알림 | P1 "트렌드 한눈에"·체류↑ | 中 — 스케줄러 + 계정 | 준희님 체류 영역 |
  | E7 개인화 | 양 페르소나 | 中 — 계정·상태 전제 | 준희님 개인화 영역 |

  > 잠정 1순위: **E5**(P1 핵심 아픔 + 제품 차별화). 단 셋 다 동결 재개방(준희님)이 선행.

### 11.C [타 역할 결정] — 나는 안건으로 올림 (가져갈 입력만 챙김)

- [ ] **실행환경** (Lambda→상시 컨테이너+큐+스케줄러, §6) — **석현님(Architect)**. *내 입력: 에이전트 런타임 요구사항(장시간·비동기·상태)*
- [ ] **로그인/계정** (NFR-SEC-01 = 동결, §9) — **준희님(PM)** + 석현님(구현) + 변경절차
- [ ] **분야 확장(A5) · E5·E6·E7 재개방** (동결, §4.5) — **준희님(PM)** + 변경절차  *(E7 개인화·E5 차별화 경험·E6 체류가 준희님 영역과 직결)*
- [ ] **비용 정책 수치** (작업당 한도·쿼터, §7) — **화랑님(수익화)** + 준희님(PM). *내 입력: U3 한 요청 약 8~18콜*
- [ ] **로그 수집·관측 정책** (작업별 토큰·비용·단계, NFR-OBS / §8·§9) — **화랑님(로그 수집)**. *내 입력: 에이전트가 내보낼 텔레메트리 항목*
- [ ] **eval 골든셋 구축** (§9) — **유진님 주도** + 팀 데이터·라벨링 공수

