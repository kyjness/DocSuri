# 파이프라인

> 사용자 대면 경로에 있는 **모듈 4개**를 단락으로 나눈다. 각 단락은 그 모듈의 **대표 경로
> 1건**(논문 1편 수집 · 검색 1회 · 요약 1건 · 질문 1턴)을 끝까지 따라가고, 갈라지는 길은
> 곁다리 박스로 잇는다.
>
> 본문의 모든 수치(top 150 · RRF k=60 · 청크 30,000토큰 · 상한 8회 …)는 **코드 상수 그대로**다 —
> 추정값이 아니다.
>
> **그림은 모듈마다 모양이 다르다.** 같은 틀에 넣지 않았다 — U1은 이른 탈출이 있는 관문
> 통로, U2는 갈렸다 다시 만나는 포크, U7은 관문 하나에 기계 둘, U11은 되도는 루프다. 모양
> 자체가 그 모듈이 무엇인지 말한다.
>
> **읽는 법:** `╔═╗` = 판정이 갈리는 관문 · 게이트 / `┌─┐` = 일반 단계 · 곁다리 /
> `[ … ]` = 외부 인프라 · 다른 모듈 / `★` = 분기 · 폴백 · 조용한 실패를 깨는 자리 /
> `──▶` = 외부 호출 / 가지가 갈리는 자리는 `┬` `┴` `╪` 로 붙는다.

---

## 0 · 모듈 한눈에

| 모듈 | 한 줄 | 파이프라인 모양 | 핵심 불변식 |
|---|---|---|---|
| **U1** ingestion | arXiv 수집 → 파싱 → doc-model → 색인 | **관문 통로** — 관문마다 빠지는 길이 있고, 빠지는 것이 정상이다 | 중복은 빌드 **전에** 끊는다 |
| **U2** discovery | 하이브리드 검색 → 리랭크 → 근거 검사 | **포크** — 두 다리가 동시에 돌고 RRF에서 만난다 | 인덱스는 폴백이 없다(fail-closed) |
| **U7** summarization | 요약·번역 + 앵커 검증 | **관문 + 기계 둘** — 캐시가 먼저 답하고, 미스면 둘 중 하나 | 캐시 키가 용어집을 탄다 |
| **U11** evidence | 근거형성 에이전트 | **루프 + 출구 관문** — 끝내려면 바닥 둘을 지나야 한다 | 근거 없으면 답을 안 낸다 |

**공통 규칙 하나**: 실패는 조용하면 안 된다. 이 저장소가 반복해서 데인 것이 "결과가 적을 뿐"으로
보이는 열화라, 각 단락의 `★`는 대부분 **그 조용함을 깨는 자리**다.

---

## 1 · 모듈 의존 관계

화살표는 `부르는 쪽 → 불리는 쪽`이다.

```
   U1 ingestion ── 아무도 안 부른다. 큐가 유일한 입구다
   ┌──────────────┐
   │  ingestion   │◀── [ SQS ]  ◀── U7(본문 없음) · U11(색인 요청) · 수집 배치
   └──────┬───────┘
          │ 산출물로만 잇는다 — import 하지 않는다
          ▼
   [ OpenSearch 색인 ] · [ S3 doc-model · 자산 ] · [ Postgres paper_asset ]
          │                        │
          ▼                        ▼
   ┌──────────────┐        ┌──────────────────┐
   │  discovery   │        │  summarization   │
   └──────┬───────┘        └────────┬─────────┘
          │                                   │
          └──────────┬────────────────────────┘
                     ▼
            ┌──────────────┐
            │   evidence   │  U2를 검색 포트로, U7을 본문·자산 포트로 부른다
            └──────────────┘

   ★ evidence만 다른 모듈을 부른다. 나머지 셋은 서로를 모른다 — 산출물(색인·S3·DB)로만
     이어져 있어, 한 모듈을 갈아끼워도 나머지가 안 바뀐다. U1이 파서를 바꿔도 U2는 색인만 본다.
```

---

## 2 · 계층 구조

네 모듈이 전부 같은 층이다. 하나만 이해하면 나머지가 읽힌다.

```
╔══ backend/modules/<모듈>/ (U1은 ingestion/src/) ═════════════════════════════════════════════════╗
║                                                                                                  ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ api · controller     HTTP 표면만                       │  DTO 검증 → 서비스 호출              ║
║  │    인증은 U6 미들웨어가 끝낸 뒤 진입한다               │  트랜잭션을 열지 않는다              ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ service              오케스트레이션 · 예산 · 저하 판정 │  외부를 직접 안 만진다               ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ domain               ★ 순수 로직 — 여기에 I/O가 없다   │  게이트 · 랭킹 · 루프 · 청킹         ║
║  │    같은 입력이면 같은 출력. 테스트가 대본으로 돈다     │                                      ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ ports                프로토콜 선언만                   │  domain이 바깥을 부르는 유일한 창    ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ OpenSearch · S3 · Bedrock ]   ║
║  │ adapters             실제 왕복 · 재시도 · 회로차단기   │  real_wiring.py 가 조립한다          ║
║  └────────────────────────────────────────────────────────┘                                      ║
╚══════════════════════════════════════════════════════════════════════════════════════════════════╝
```

**domain에 I/O가 없다는 규칙 하나가 꽤 많은 걸 결정한다.** evidence의 루프는 도구 호출을 직접
하지 않고 포트로 제안만 낸다 — 그래서 골든셋 51문항이 **Bedrock 호출 0회로** CI에서 돈다.
어댑터가 각자 판단할 수 있었다면 그 검사를 만들 수 없었다.

---

## 3 · U1 ingestion — 관문을 통과해야 색인에 오른다

**모양: 이른 탈출이 있는 관문 통로.** 논문 1편이 관문 넷을 지나야 색인에 오르고, 각 관문에서
빠지는 길이 따로 있다. 빠지는 것이 실패가 아니라 **정상 경로**라는 게 이 모듈의 성격이다.

입구는 큐 하나다. 잡은 다섯 종류이고 대표 경로는 `EVENT`(논문 1편 지정 수집)다.

| 잡 | 언제 | 어디로 |
|---|---|---|
| `SEED_REBUILD` | 코퍼스 처음 만들 때 | 아래 통로 |
| `INCREMENTAL` | 날짜 창 수확(`CORPUS_WINDOW_DAYS = 135`) | 아래 통로 |
| **`EVENT`** | **논문 1편 지정** | **아래 통로** ← 대표 |
| `BUILD_DOC_MODEL` | 본문 캐시 미스(U7·U11이 올린다) | **아래 통로를 안 탄다** — 별도 진입점(`build_doc_model`)으로 사다리·빌드만 돈다. **이미 색인된 논문 전용** |
| `BUILD_USER_DOC_MODEL` | 사용자 업로드 PDF | 같은 별도 진입점 — `arxivRef` 없이 S3에서 읽는다 |

```
                                 [ SQS ]
                                    │
                                    ▼
   ╔══ ① 메타데이터 ════════════════════════════════════════╗
   ║   제목 · 초록 · 저자 · 카테고리   ──▶ [ arXiv OAI ]    ║
   ║   묶음 수확이 이미 실었으면 왕복을 건너뛴다            ║
   ║   (arXiv 정중함 예산)                                  ║
   ╚═══════════════════════╤════════════════════════════════╝
                           ▼
   ╔══ ② 정규 키 → 중복 판정 ══════════════════════════════╗
   ║   canonical_dedup_state                               ║
   ║   begin_upsert 로 동시 재배달을 흡수한다              ║
   ╚═══╤═══════════════╤═══════════════════════════════════╝
       │ DUPLICATE     │ STALE            │ NEW · CHANGED
       ▼               ▼                  │
   ┌─────────┐     ┌─────────┐            │  ★ 여기서 빠지는 둘은 성공이다.
   │ 끝(성공)│     │ 끝(성공)│            │    빌드·임베딩 비용을 한 푼도 안 낸다
   └─────────┘     └─────────┘            │
                                          ▼
   ╔══ ③ 본문 사다리 ══════════════════════════════════════╗
   ║                                                       ║
   ║     ar5iv HTML  ──▶ [ ar5iv ]                         ║
   ║         │ 실패 · 잘린 변환                            ║
   ║         ▼                                             ║
   ║     arXiv PDF   ──▶ [ arXiv ] ──▶ [ GROBID ] → TEI    ║
   ║         │ 실패                                        ║
   ║         ▼                                             ║
   ║     ★ 제외 — 전문을 아예 안 남긴다  ─────────────────╫──▶ ┌──────────┐
   ║                                                       ║    │ 끝(제외) │
   ║     native arXiv HTML 은 두 사다리에서 제거됐다        ║    └──────────┘
   ╚═══════════════════════╤═══════════════════════════════╝
                           ▼  한 단이라도 성공
   ╔══ ④ doc-model 빌드 ════════════════════════════════════╗
   ║   문단 · 표 · 수식 · 그림 crop — 블록마다 id           ║
   ║   그 id 가 나중에 인용 앵커의 대상이 된다              ║
   ║   ★ 중복 판정 뒤, 전문 저장 앞에 돈다                  ║
   ╚═══════════════════════╤════════════════════════════════╝
                           ▼
   ╔══ ⑤ 청킹 → 임베딩 → 색인 ══════════════════════════════╗
   ║   doc-model 기준 청킹  ──▶ [ Bedrock embed ]           ║
   ║   벡터 + BM25 + 자산    ──▶ [ OpenSearch · S3 · PG ]   ║
   ║   수확 워터마크(시각) 전진                             ║
   ║   — 다음 증분 수확이 여기서 재개한다                   ║
   ╚════════════════════════════════════════════════════════╝

   실패는 두 갈래로만 나간다
       RETRIABLE  ──▶ 큐로 되돌림
       PERMANENT  ──▶ [ DLQ ]
       ★ 섞으면 못 고칠 잡이 영원히 돈다
```

| 왜 | |
|---|---|
| **중복을 관문 ②에 둔다** | ③④⑤ 뒤였다면 재배달된 잡이 매번 파싱·임베딩 비용을 낸다. 큐는 at-least-once라 재배달이 **정상**이다 |
| **④가 전문 저장보다 앞** | 사다리가 전부 실패한 논문은 전문을 안 남긴다. 쓰고 지우는 것보다 안 쓰는 것이 낫다 — 흔들리는 GROBID가 재배달 횟수만큼 쓰기를 늘리지 않고, 재파싱이 남은 찌꺼기를 안 만난다 |
| **native arXiv HTML 제거** | 그 HTML은 LaTeX 매크로가 그대로 남아, 색인이 청킹하는 평문에 매크로가 **글자 그대로** 들어갔다 |

> ┌ **곁다리 — 배포 박스에는 이 통로가 없다** ───────────────────────────────────────────────┐
> │ GROBID(1.84 GB)·Docling은 로컬에서만 돈다. 배포에는 **색인 결과만** 올린다(질문지 Q7=B).  │
> │ 그래서 데모에는 `EVENT` 잡을 넣을 큐도 소비할 워커도 없다 — "쓸수록 코퍼스가 자란다"는    │
> │ 배포본에서 성립하지 않는다. 의도된 것이다.                                                │
> └──────────────────────────────────────────────────────────────────────────────────────────┘

---

## 4 · U2 discovery — 두 다리로 갈렸다가 합쳐진다

**모양: 갈라졌다 다시 만나는 포크.** 의미 검색과 어휘 검색이 **동시에** 돌고 RRF가 합친다.
그 위에 저하 모드가 겹쳐서, **어느 다리가 도는지 자체가 바뀐다.**

```
   POST /api/search
        │
        ▼
   ┌──────────────────────┐   ★ 부적합 질의 → 끝(오류)
   │ 검증 · 정규화        │
   └──────────┬───────────┘
              ▼
   ┌──────────────────────────────────────┐  ──▶ [ U6 cost guard ]
   │ 예산 조회 → 저하 모드 결정           │
   └──────────┬───────────────────────────┘
                                          │
              │   저하 모드가 어느 다리를 돌릴지를 정한다
              │     NORMAL       k-NN + BM25 + 리랭크
              │     RERANK_OFF   k-NN + BM25
              │     LEXICAL_ONLY BM25만 — 임베딩을 아예 안 부른다
              ▼
   ┌──────────────────────────────────────┐  ──▶ [ Bedrock embed ]
   │ 질의 확장 · 임베딩                   │
   │  ★ 실패 → LEXICAL_ONLY 로 내려앉고   │  ★ 이때 리랭크도 같이 끈다
   │    경고 로그를 남긴다                │     (같은 provider라 어차피 스로틀)
   └──────────┬───────────────────────────┘
                                          │
   ═══════════╪═════════════════════════════════ 여기서 두 다리로 갈린다
                       │
      ┌───────┴────────┐
      ▼                ▼
   ┌───────────┐   ┌──────────────────────┐  ──▶ [ OpenSearch ]
   │ k-NN      │   │ BM25                 │
   │ top 150   │   │ top 150              │      LEXICAL_ONLY 면
   │           │   │                      │      왼쪽 다리가 통째로 없다
   │ LITE 초록 │   │ LITE 제목·초록       │      ★ 다리가 하나면 그것이 곧
   │ FULL 전체 │   │ FULL + 어휘 필드     │        순위다 — RRF가 할 일이 없다
   └─────┬─────┘   └──────────┬───────────┘
         │                                │
         │ ★ 원점수를 여기서 챙긴다                     ⑦이 이 값을 쓴다
         │   융합이 절대 관련도를 버리기 때문이다
         │                    │
         └─────────┬──────────┘
                   ▼
   ┌──────────────────────────────────────┐
   │ RRF 융합   1/(60 + rank + 1) 합산    │  논문별 중복 제거
   └──────────┬───────────────────────────┘
              ▼
   ┌──────────────────────────────────────┐  ──▶ [ Bedrock rerank ]
   │ 리랭크 · 개인화 부스트               │  ★ 실패는 soft — 융합 순위 유지
   └──────────┬───────────────────────────┘
              ▼
   ┌──────────────────────────────────────┐
   │ ⑦ 근거 없음 바닥                     │  ★ k-NN 원점수가 바닥 밑이면
   └───┬──────────────────────┬───────────┘     결과를 안 낸다
       │ 통과                 │ 미달
       ▼                      ▼
   결과 + (저하 배너)    "관련 논문 없음"
```

| 왜 | |
|---|---|
| **원점수를 융합 전에 챙긴다** | RRF는 상대 순위만 남기고 절대 관련도를 버린다. 순위는 아무리 나쁜 후보라도 1등을 만드므로, "정말 관련 있는가"는 융합 **전** 점수로만 판정된다 |
| **LITE가 기본** | 사람이 쓰는 검색창은 초록 청크만 본다(논문당 1개 — 빠르고 논문 단위 매칭). FULL은 본문 청크까지 — 에이전트가 쓴다 |
| **임베딩 실패를 로그로 남긴다** | 저하는 화면에 배너로 나가지만 **로그에는 아무것도 안 남았다.** 서버 쪽에서 스로틀과 정상 검색이 구분되지 않아 리랭크 A/B 측정이 오염됐다(2026-08-15) |
| **인덱스만은 폴백이 없다** | 임베딩·리랭크는 저하로 견딘다. OpenSearch가 죽으면 **실패로 끝낸다** — 검색 결과를 지어낼 대체 경로가 없다 |

---

## 5 · U7 summarization — 캐시 관문 뒤에 기계가 둘

**모양: 관문 하나 + 갈라진 두 기계.** 캐시가 먼저 답하고, 미스일 때만 요약·번역 중 **하나**가
돈다. 둘은 뼈대가 같고 상수가 다르다.

```
   POST /api/papers/{id}/summary       task = SUMMARY | TRANSLATE
        │
        ▼
   ┌────────────────────────────────────────────┐
   │ 용어집 해석    강한 용어 = 프롬프트에 박힘  │  약한 용어 = 읽을 때 덧씌움
   └─────────┬──────────────────────────────────┘     → 캐시를 안 가른다
             ▼
   ╔════════════════════════════════════════════╗  ──▶ [ S3 요약 캐시 ]
   ║ 캐시 조회                                  ║
   ║   키 = 논문·버전·파서·용어 서명            ║
   ╚═══╤════════════════════════════╤═══════════╝
       │ HIT                        │ MISS
       ▼                            ▼
   ┌───────────────┐    ┌────────────────────────────────┐  ──▶ [ S3 doc-model ]
   │ 그대로 반환    │    │ 원문 확보                      │
   │ LLM 0회   │    │  ★ 없음 → U1에 빌드 잡 + 기권  │──▶ 끝(pending)
   └───────────────┘    └───────────┬────────────────────┘
                                    ▼
                        ┌────────────────────────────────┐
                        │ 정제 → 청킹                    │
                        └───┬────────────────────────┬───┘
                            │ 청크 1개               │ 청크 여러 개
                            ▼                        ▼
                     ┌─────────────┐      ┌─────────────────────────┐
                     │ 1회 호출     │      │ map(동시)  →  reduce   │
                     │ 팬아웃 없음  │      │  ★ 순서 보존           │
                     └──────┬──────┘      │  ★ 실패는 즉시 중단    │
                            │             └───────────┬────────────┘
                            └──────────┬──────────────┘
                                       ▼
                        ╔══════════════════════════════════════╗
                        ║ 앵커 검증(게이트)                    ║
                        ║  앵커가 원문 블록을 가리키는가       ║
                        ║  숫자 불일치 임계 0.4                ║
                        ║  ★ 못 맞추면 그 앵커를 떨어뜨린다    ║
                        ╚══════════════════════════════════════╝

   두 기계의 상수가 다르다
   ┌──────────────┬───────────────┬────────┬────────────┬──────────────────────────┐
   │              │ 청크 예산     │ 겹침   │ 동시 호출  │ 산출                     │
   ├──────────────┼───────────────┼────────┼────────────┼──────────────────────────┤
   │ 요약         │ 30,000 토큰   │ 2,000자│ 3      │ tldr·기여·방법·결과·         │
   │              │               │        │            │ 한계·재현성·앵커         │
   │ 번역         │  3,500 토큰   │ —      │ 4      │ 원문 구조 그대로             │
   │              │               │        │ 재시도 2회 │                          │
   └──────────────┴───────────────┴────────┴────────────┴──────────────────────────┘
   ★ 요약 청크가 크고(30K) 번역 청크가 작아(3.5K) 동시 수가 반대로 붙는다 —
     요약은 몇 개만 던져도 Bedrock 토큰/분 한도를 밀어붙인다.

   긴 요약은 요청 경로에서 안 돈다
        API 경로  ──▶ 잡 큐에 올리고 pending 반환   (allow_enqueue=True)
        워커 경로 ──▶ map-reduce를 인라인으로       (allow_enqueue=False)
        ★ 요청 경로에서 돌리면 게이트웨이 타임아웃이 먼저 끊는다
```

| 왜 | |
|---|---|
| **캐시 키가 용어집을 탄다** | 강한 용어는 프롬프트에 박히므로 바뀌면 결과가 달라진다. 약한 용어는 읽을 때 덧씌우므로 캐시를 안 가른다 — 안 그러면 사용자마다 같은 요약을 다시 만든다 |
| **재현성이 필수 필드** | 스키마가 `code`·`data`를 요구해 모델이 뺄 수 없다. 종전에는 논문에 없을 때 **빈 문자열**이 나가 화면에서 "논문에 없음"과 "모델이 못 찾음"이 구분되지 않았다 — 프롬프트가 `논문에 언급 없음`을 정확히 쓰게 하고 화면은 빈 값을 "확인하지 못했어요"로 낸다 |

---

## 6 · U11 evidence — 루프, 그리고 나가는 문의 관문

**모양: 되도는 루프 + 출구 관문.** `decide`가 도구를 부르는 한 계속 돌고, 끝내려면 관문 둘을
지나야 한다. 관문에 걸리면 **사유를 관찰에 실어 루프로 되돌린다.**

LangGraph 그래프이고 체크포인터는 Postgres, `thread_id = turn_id`다.

**턴은 요청 경로에서 안 돈다.** 접수는 즉시 끝나고 실행은 따로 간다 — 한 턴이 30~90초라
요청을 붙들면 게이트웨이가 먼저 끊는다.

```
   POST /api/evidence/turns
        │
        ▼
   ┌────────────────────────────┐
   │ 접수 — 세션·턴 행 생성      │──▶ 즉시 { turnId, state: pending } 반환
   └─────────┬──────────────────┘
             │ 실행을 넘긴다
             ▼
   ┌────────────────────────────┐        클라이언트는 두 길로 결과를 본다
   │ 실행자                     │        ┌──────────────────────────────────┐
   │  local  — API 프로세스의   │        │ GET .../turns/{id}/events  (SSE) │
   │          스레드 풀 ← 데모  │◀──────▶│   단계 이름 · 결과 요약 · 소요초 │
   │  sqs    — 별도 워커        │        │ GET .../turns/{id}       (폴링)  │
   └─────────┬──────────────────┘        └──────────────────────────────────┘
             ▼                            ★ Caddy가 SSE를 버퍼링하면 이벤트가
        아래 그래프가 돈다                  턴 끝에 한꺼번에 온다 — 에이전트가
                                            느린 것과 구분되지 않는다
```

```
   START
     │
     ▼
   ┌─────────────────────────────────────────────────────────────────────┐
   │                             decide                                  │
   │   관찰(확보한 논문 · 근거 · 남은 예산 · 거부 사유)을 보고 다음 수   │
   └───┬─────────────────────────────────────────────────┬───────────────┘
       │ 도구 제안                                       │ 종료 제안
       ▼                                                 │
   ┌────────────────────────────────────────────┐        │
   │                    act                     │        │
   │  corpus_search 5   live_lookup 3           │        │
   │  fetch_paper   8   read_paper  8           │        │
   │  view_figure   6   extract_evidence 8      │        │
   │                                            │        │
   │  ★ 상한 초과 · 인자 오류 → 도구를 안 돌리고│        │
   │    거부 사유를 관찰에 실어 되돌린다    │            │
   │                                            │        │
   │  ★ extract_evidence 는 논문을 여러 편 받으면        │
   │    **논문별로 나눠 동시에** 던진다(상한 4).│        │
   │    턴 시간의 67~80%가 추출이고 묶은 호출이 │        │
   │    가장 느리다 — 3편 35.8초 vs 1편 22초.   │        │
   │    한 편이 죽어도 나머지는 살리고, 실패한  │        │
   │    논문 id를 모델에게 알린다.              │        │
   └───────────────────┬────────────────────────┘        │
                       │ 새 관찰                          │
                       └──────────▶ decide 로 되돌아감   │
                                                          ▼
   ╔══════════════════════════════════════════════════════════════════╗
   ║                          check_floor                             ║
   ║                                                                  ║
   ║   바닥 1  근거가 0건인가                                         ║
   ║   바닥 2  주장·비교형인데 stance=counter 탐색이 0회인가          ║
   ║   탈출구  stance 도구를 더 부를 예산이 없으면 통과시킨다         ║
   ╚═══╤══════════════════════════════════════════╤═══════════════════╝
       │ 미달 — 사유를 관찰에 실어 decide 로       │ 통과
       └──────────▶ (루프로)                       ▼
                                    ┌──────────────────────────────────┐
                                    │            answer                │
                                    │  게이트를 통과한 근거만 본다     │
                                    └───────────────┬──────────────────┘
                                                    ▼
                                   ┌─────────────────────────────────────┐
                                   │ 문장 검사 5종                       │
                                   │                                     │
                                   │  A1 번호 실재 ┐                     │
                                   │  A2 숫자 근거 ┴▶ 강등               │
                                   │                 (문장은 남고        │
                                   │                  인용 표시만 잃음)  │
                                   │                                     │
                                   │  A3 없는 논문 ┐                     │
                                   │  A4 인용 0건  ┼▶ 거부               │
                                   │  A5 종합 0.5  ┘   → 재생성 1회      │
                                   │                   → 또 거부면 폴백  │
                                   └───────────────┬─────────────────────┘
                                                   ▼
                                                  END

   턴 상한: 반복 12회 · 비용 $1.50 · 정체 판정 600초 · 체크포인트 보존 7일
   ★ 상한이 $0.50이던 동안 **추출 비용이 장부 밖에 있어** 사실상 안 걸렸다. 계상을 고치고
     재보니 평범한 턴 하나가 $0.269 — 옛 상한의 54%다. 그대로 뒀으면 완주하던 턴이 중간에
     끊겼을 것이다: 계상을 바로잡은 것이 사용자에게는 기능 후퇴로 보인다.
   기권 사유는 닫힌 어휘 9종이다. 앞 다섯은 근거형성이 낸 기권, 뒤 넷은 턴 실패가
   fail-closed로 수렴한 것 — 여기 없는 코드는 unknown으로 모이고 원래 값은 로그에 남는다.
     out_of_corpus · insufficient_evidence · cost_degraded · cancelled · llm_unavailable
     internal_error · dispatch_failed · session_unavailable · unknown
   ★ 그냥 string이던 동안 백엔드 생산자와 화면 라벨 맵이 조용히 갈렸다(2026-08-24).
```

**근거가 answer에 닿기 전에 지나는 게이트**가 이 모듈의 핵심이다. 위 그림의 "게이트를 통과한
근거만"이 가리키는 것이 이것이다.

```
   extract_evidence 가 뽑아 온 후보
        │
        ▼
   ┌──────────────────────────────────────────────┐
   │ span 대조   인용문이 원문 블록에 실재하는가  │──▶ 없으면 그 근거를 버린다
   └──────────────┬───────────────────────────────┘
                  ▼
   ┌──────────────────────────────────────────────┐
   │ 숫자 대조   문장의 숫자가 근거에 있는가      │──▶ number_not_grounded
   └──────────────┬───────────────────────────────┘
                  ▼
        통과한 것만 answer 로
```

| 왜 | |
|---|---|
| **거부가 아니라 되돌리기** | 종료를 거부만 하면 모델은 같은 제안을 반복하고 그 반복이 예산을 태운다. **사유를 관찰에 실어** 돌려보내면 다음 수가 달라진다 |
| **stance가 프롬프트가 아니라 인자** | "반대 근거도 찾아라"는 당부는 안 지켜지는 것이 실측됐다. 인자로 두면 지켜졌는지가 트레이스에서 **기계로 세어진다** |
| **바닥에 탈출구가 있다** | 예산이 소진된 주장형 턴은 반대 측을 더 찾을 수가 없다. 탈출구가 없으면 종료 제안이 매번 거부되며 남은 반복을 LLM 호출로 태우고 `budget_exhausted`로 끝난다 |
| **강등과 거부를 나눈다** | 번호·숫자가 안 맞는 것은 문장을 남기고 인용 표시만 뺀다. 없는 논문이 등장하는 것은 **지어낸 것**이라 답변 전체를 물린다 |
| **판단은 게이트 통과분만 본다** | 떨어진 근거를 보여 주면 모델이 그것을 근거로 쓴다. 게이트가 두 번 열리는 셈이 된다 |

> ┌ **곁다리 — 코퍼스에 없는 논문** ─────────────────────────────────────────────────────────┐
> │                                                                                          │
> │   live_lookup ──┬──▶ [ arXiv ]        ┐                                                  │
> │                 ├──▶ [ Semantic Scholar ] ├─ **동시에** 친다 · 소스별 회로차단기          │
> │                 └──▶ [ OpenAlex ]     ┘                                                  │
> │                          │                                                               │
> │                          ▼  중복 제거(arXiv id 우선) · 승자 arXiv > S2 > OA               │
> │                    후보 목록 + **죽은 소스 목록**                                        │
> │                                                                                          │
> │   ★ 하나가 죽어도 나머지로 진행하고 **셋 다 죽었을 때만** 조회 실패다.                    │
> │   ★ 죽은 소스는 모델 관찰과 화면 양쪽에 밝힌다 — 소스가 죽어 못 찾은 것과 세상에 없는     │
> │     것은 **사용자가 할 일이 정반대**다. 소스 이름은 안 싣는다(SEC-9).                     │
> │   ★ 결과는 **초록까지**. 본문은 승격이 담당하는데 배포본에는 그 워커가 없다.              │
> └──────────────────────────────────────────────────────────────────────────────────────────┘

---

## 7 · 검증 층

네 모듈이 같은 방식으로 검사된다 — **domain에 I/O가 없어서** 가능한 구조다.

| | 무엇을 재나 | 비용 |
|---|---|---|
| 단위 · PBT | 게이트 · 랭킹 · 청킹 · 루프 라우팅 | 0 |
| **1층 골든셋**(U11) | 51문항 — 배선과 불변식. 녹화 대역이라 **CI에서 LLM 0회** | 0 |
| **2층 심판**(U11) | 실 스택·실 코퍼스로 문항마다 턴 1회 + 심판 1회 | Bedrock 호출 |
| 실스택 스모크 | 배포본에서 손으로 — 편수 · 전문 · 그림 · 턴 1회 | 실비용 |

**1층 수치를 품질로 읽으면 안 된다.** 대본이 기대대로 움직이므로 recall@k가 **구조적으로**
1.00이다. 재는 것은 배관이고, 1.00에서 떨어지면 품질이 아니라 배선이 깨진 것이다. 품질은
2층이 본다 — 같은 지표가 실 코퍼스에서는 **0.556**이었다(3,248편이 실제로 답할 수 있는 범위).
