# 파이프라인

> 사용자 대면 경로에 있는 **모듈 4개**를 단락으로 나눈다. 각 단락은 그 모듈의 **대표 경로
> 1건**(논문 1편 수집 · 검색 1회 · 요약 1건 · 질문 1턴)을 끝까지 따라가고, 갈라지는 길은
> 곁다리 박스로 잇는다.
>
> 본문의 모든 수치(top 150 · RRF k=60 · 청크 30,000토큰 · 상한 8회 …)는 **코드 상수 그대로**다 —
> 추정값이 아니다.
>
> **읽는 법:** `╔═╗` = 대표 경로(본류) / `┌─┐` = 곁다리 · 모듈 밖 / `[ … ]` = 외부 인프라 ·
> 다른 모듈 / `★` = 폴백 · 게이트 등 분기점 / `──▶` = 외부 호출.

---

## 0 · 모듈 한눈에

| 모듈 | 한 줄 | 경로 성격 | 핵심 불변식 |
|---|---|---|---|
| **U1** ingestion | arXiv 수집 → 파싱 → doc-model → 색인 | 비동기 잡(SQS) | 중복은 빌드 **전에** 끊는다 |
| **U2** discovery | 하이브리드 검색 → 리랭크 → 근거 검사 | 동기 | 인덱스는 폴백이 없다(fail-closed) |
| **U7** summarization | 요약·번역 map-reduce + 앵커 검증 | 동기 + 긴 건은 잡 | 캐시 키가 용어집을 탄다 |
| **U11** evidence | 근거형성 에이전트 루프 | 비동기 턴 + SSE | 근거 없으면 답을 안 낸다 |

**공통 규칙 하나**: 실패는 조용하면 안 된다. 이 저장소가 반복해서 데인 것이 "결과가 적을 뿐"으로
보이는 열화라, 각 단락의 `★`는 대부분 **그 조용함을 깨는 자리**다.

---

## 1 · 모듈 의존 관계

화살표는 `부르는 쪽 → 불리는 쪽`이다.

```
   U1 ingestion ── 아무도 안 부른다. 큐가 유일한 입구다
   ┌──────────────┐
   │  ingestion   │◀── [ SQS ]  ◀── U7(본문 없음) · U11(색인 요청) · 수집 배치
   └──────┬───────┘
          │ 산출물로만 잇는다 — import 하지 않는다
          ▼
   [ OpenSearch 색인 ] · [ S3 doc-model · 자산 ] · [ Postgres paper_asset ]
          │                        │
          ▼                        ▼
   ┌──────────────┐        ┌──────────────────┐
   │  discovery   │        │  summarization   │
   └──────┬───────┘        └────────┬─────────┘
          │                         │
          └──────────┬──────────────┘
                     ▼
            ┌──────────────┐
            │   evidence   │  U2를 검색 포트로, U7을 본문·자산 포트로 부른다
            └──────────────┘

   ★ evidence만 다른 모듈을 부른다. 나머지 셋은 **서로를 모른다** — 산출물(색인·S3·DB)로만
     이어져 있어, 한 모듈을 갈아끼워도 나머지가 안 바뀐다. U1이 파서를 바꿔도 U2는 색인만 본다.
```

---

## 2 · 계층 구조

네 모듈이 전부 같은 층이다. 하나만 이해하면 나머지가 읽힌다.

```
╔══ backend/modules/<모듈>/ (U1은 ingestion/src/) ═════════════════════════════════════════════════╗
║                                                                                                  ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ api · controller     HTTP 표면만                       │  DTO 검증 → 서비스 호출              ║
║  │    인증은 U6 미들웨어가 끝낸 뒤 진입한다               │  트랜잭션을 열지 않는다              ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ service              오케스트레이션 · 예산 · 저하 판정 │  외부를 직접 안 만진다               ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ domain               ★ 순수 로직 — 여기에 I/O가 없다   │  게이트 · 랭킹 · 루프 · 청킹         ║
║  │    같은 입력이면 같은 출력. 테스트가 대본으로 돈다     │                                      ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ ports                프로토콜 선언만                   │  domain이 바깥을 부르는 유일한 창    ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ OpenSearch · S3 · Bedrock ]   ║
║  │ adapters             실제 왕복 · 재시도 · 회로차단기   │  real_wiring.py 가 조립한다          ║
║  └────────────────────────────────────────────────────────┘                                      ║
╚══════════════════════════════════════════════════════════════════════════════════════════════════╝
```

**domain에 I/O가 없다는 규칙 하나가 꽤 많은 걸 결정한다.** evidence의 루프는 도구 호출을 직접
하지 않고 포트로 제안만 낸다 — 그래서 골든셋 51문항이 **Bedrock 호출 0회로** CI에서 돈다.
어댑터가 각자 판단할 수 있었다면 그 검사를 만들 수 없었다.

---

## 3 · U1 ingestion — 논문 1편이 색인에 오르기까지

입구는 큐 하나다. 잡 종류는 다섯이고, 대표 경로는 `EVENT`(논문 1편 수집)다.

| 잡 | 언제 |
|---|---|
| `SEED_REBUILD` | 코퍼스 처음 만들 때 |
| `INCREMENTAL` | 날짜 창 수확(`CORPUS_WINDOW_DAYS = 135`) |
| **`EVENT`** | **논문 1편 지정 수집** ← 대표 경로 |
| `BUILD_DOC_MODEL` | 본문 캐시 미스 — U7 뷰어·요약이 올린다. **이미 색인된 논문 전용** |
| `BUILD_USER_DOC_MODEL` | 사용자가 올린 PDF |

```
╔══ ingest_one(job) ═══════════════════════════════════════════════════════════════════════════════╗
║                                                                                                  ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ arXiv OAI-PMH ]               ║
║  │ ① 메타데이터        제목 · 초록 · 저자 · 카테고리      │  묶음 수확이 이미 실었으면          ║
║  │                                                        │  왕복을 건너뛴다(정중함 예산)        ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ ② 정규 키 · 중복 판정                                  │  canonical_dedup_state               ║
║  │    ★ DUPLICATE / STALE → **여기서 끝난다**             │  begin_upsert 로 동시 재배달 흡수    ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼  NEW · CHANGED 만 통과                                                  ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ ar5iv ] ──▶ [ arXiv PDF ]     ║
║  │ ③ 본문 사다리        ar5iv HTML  →  PDF/GROBID         │  ★ 둘 다 실패 → **제외**            ║
║  │    native HTML은 두 사다리에서 **제거됐다**            │     (전문을 아예 안 남긴다)          ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ ④ doc-model 빌드     문단 · 표 · 수식 · 그림 crop      │  블록마다 id — 나중에 **인용 앵커**  ║
║  │    ★ 중복 판정 **뒤**, 전문 저장 **앞**에 돈다         │  가 이 id를 가리킨다                 ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ Bedrock embed ]               ║
║  │ ⑤ 청킹 → 임베딩      doc-model 기준으로 자른다         │  청크 = 색인의 최소 단위             ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ OpenSearch · S3 · Postgres ]  ║
║  │ ⑥ 색인 · 저장        벡터 + BM25 + 자산 등록           │  watermark 로 진행 지점 기록         ║
║  └────────────────────────────────────────────────────────┘                                      ║
╚══════════════════════════════════════════════════════════════════════════════════════════════════╝
```

| 왜 | |
|---|---|
| **중복을 빌드 전에 끊는다** | 순서가 반대면 재배달된 잡이 매번 파싱·임베딩 비용을 낸다. 큐는 at-least-once라 재배달이 정상이다 |
| **doc-model이 전문 저장보다 앞** | 사다리가 전부 실패한 논문은 전문을 **아예 안 남긴다.** 쓰고 지우는 것보다 안 쓰는 것이 낫다 — 흔들리는 GROBID가 재배달 횟수만큼 쓰기를 늘리지 않고, 재파싱이 남은 찌꺼기를 안 만난다 |
| **native arXiv HTML 제거** | 그 HTML은 LaTeX 매크로가 그대로 남아, 색인이 청킹하는 평문에 매크로가 **글자 그대로** 들어갔다 |
| 실패 분류가 둘 | `RETRIABLE`은 다시 큐로, `PERMANENT`는 **DLQ로**. 섞으면 못 고칠 잡이 영원히 돈다 |

> ┌ **곁다리 — 파싱 스택은 배포 박스에 없다** ─────────────────────────────────────────────┐
> │ GROBID(1.84 GB)·Docling은 로컬에서만 돈다. 배포에는 **색인 결과만** 올린다.            │
> │ 그래서 데모에서는 `EVENT` 잡을 넣을 큐도 소비할 워커도 없다 — "쓸수록 코퍼스가 자란다"  │
> │ 는 배포본에서 성립하지 않는다. 의도된 것이다(질문지 Q7=B).                              │
> └────────────────────────────────────────────────────────────────────────────────────────┘

---

## 4 · U2 discovery — 검색 1회

```
╔══ POST /api/search ══════════════════════════════════════════════════════════════════════════════╗
║                                                                                                  ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ ① 검증 · 정규화      ★ 부적합 질의는 여기서 끝난다     │                                      ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ U6 cost guard ]               ║
║  │ ② 예산 조회 → 저하 모드 결정                           │  NORMAL / RERANK_OFF / LEXICAL_ONLY  ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ Bedrock embed ]               ║
║  │ ③ 질의 확장 · 임베딩                                   │  ★ 임베딩 실패 → LEXICAL_ONLY        ║
║  │    ★ 저하되면 **리랭크도 같이 끈다**(같은 provider다)  │     + **경고 로그**를 남긴다         ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ OpenSearch ]                  ║
║  │ ④ 하이브리드 검색    k-NN(top 150) + BM25(top 150)     │  LITE = 초록 청크만(기본)            ║
║  │                                                        │  FULL = 본문 청크까지                ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ ⑤ RRF 융합           1/(60 + rank + 1) 합산 · 논문별   │  k-NN **원점수**는 융합 전에         ║
║  │    중복 제거                                           │  따로 챙긴다(⑦이 쓴다)              ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ Bedrock rerank ]              ║
║  │ ⑥ 리랭크 · 개인화 부스트                               │  ★ 실패는 soft — 융합 순위 유지      ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ ⑦ 근거 없음 바닥     k-NN 원점수가 바닥 밑이면         │  ★ **결과를 안 낸다**                ║
║  │    "관련 논문 없음"                                    │     순위는 항상 1등을 만든다         ║
║  └────────────────────────────────────────────────────────┘                                      ║
╚══════════════════════════════════════════════════════════════════════════════════════════════════╝
```

| 왜 | |
|---|---|
| **원점수를 따로 챙긴다** | RRF는 상대 순위만 남기고 절대 관련도를 버린다. 순위는 아무리 나쁜 후보라도 1등을 만들므로, "정말 관련 있는가"는 융합 **전** 점수로만 판정된다 |
| **LITE가 기본** | 사람이 쓰는 검색창은 초록 청크만 본다(논문당 1개 — 빠르고 논문 단위 의미 매칭). FULL은 본문까지 — 에이전트가 쓴다 |
| **임베딩 실패를 로그로 남긴다** | 저하는 화면에 배너로 나가지만 **로그에는 아무것도 안 남았다.** 서버 쪽에서 스로틀과 정상 검색이 구분되지 않아 리랭크 A/B 측정이 오염됐다(2026-08-15) |
| **인덱스는 폴백이 없다** | 임베딩은 저하로 견디지만 OpenSearch가 죽으면 **실패로 끝낸다.** 검색 결과를 지어낼 대체 경로가 없다 |

---

## 5 · U7 summarization — 요약 1건

```
╔══ POST /api/papers/{id}/summary ═════════════════════════════════════════════════════════════════╗
║                                                                                                  ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ ① 용어집 해석        사용자 개인 용어 + 표준 시드      │  강한 용어 = 프롬프트에 박힌다       ║
║  │                                                        │  약한 용어 = 읽을 때 덧씌운다        ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ S3 요약 캐시 ]                ║
║  │ ② 캐시 조회          키 = 논문·버전·파서·**용어 서명** │  ★ HIT → **LLM 0회로 끝**            ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼  MISS                                                                   ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ S3 doc-model ]                ║
║  │ ③ 원문 확보          없으면 U1에 빌드 잡을 올린다      │  ★ 없음 → **기권**(pending)          ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ ④ 정제 · 청킹        청크 예산 30,000토큰 · 겹침 2,000자│  ★ 한 청크면 팬아웃 없이 1회 호출   ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ Bedrock LLM ] × 동시 3        ║
║  │ ⑤ map → reduce       청크별 요약 → 하나로 합침         │  순서 보존 · 실패는 즉시 중단        ║
║  │    산출: tldr·기여·방법·결과·한계·**재현성**·앵커      │  전부 도구 호출로 **스키마 강제**    ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ ⑥ 앵커 검증(게이트)  앵커가 원문 블록을 가리키는가     │  숫자 불일치 임계 0.4                ║
║  │    ★ 못 맞추면 앵커를 떨어뜨린다                       │                                      ║
║  └────────────────────────────────────────────────────────┘                                      ║
╚══════════════════════════════════════════════════════════════════════════════════════════════════╝

  번역은 같은 뼈대에 상수만 다르다 — 청크 3,500토큰 · 동시 4 · 토큰 재시도 2회.
  요약 청크가 크고(30K) 번역 청크가 작아(3.5K), 동시 수가 반대로 붙는다.
```

| 왜 | |
|---|---|
| **캐시 키가 용어집을 탄다** | 강한 용어는 프롬프트에 박히므로 바뀌면 결과가 달라진다. 약한 용어는 읽을 때 덧씌우므로 **캐시를 안 가른다** — 안 그러면 사용자마다 같은 요약을 다시 만든다 |
| **요약 동시 수가 번역보다 낮다** | 요약 청크는 약 30K 토큰짜리라 몇 개만 동시에 던져도 Bedrock 토큰/분 한도를 밀어붙인다 |
| **긴 요약은 잡으로** | API 경로는 pending을 내고 워커가 인라인으로 돈다. 요청 경로에서 map-reduce를 돌리면 게이트웨이 타임아웃이 먼저 끊는다 |
| **재현성이 필수 필드** | 스키마가 `code`·`data`를 요구하므로 모델이 뺄 수 없다. 다만 **논문에 없으면 빈 문자열**이고, 화면에서 "모델이 못 찾음"과 구분되지 않는다 — 알려진 결함이다 |

---

## 6 · U11 evidence — 질문 1턴

LangGraph 그래프이고 노드가 넷이다. 체크포인터는 Postgres이고 `thread_id = turn_id`다.

```
╔══ POST /api/evidence/turns ══════════════════════════════════════════════════════════════════════╗
║                                                                                                  ║
║   START                                                                                          ║
║     │                                                                                            ║
║     ▼                                                                                            ║
║  ┌────────────┐   도구 제안   ┌────────────────────────────────────────────────────────┐         ║
║  │  decide    │──────────────▶│ act    도구를 실제로 돌린다                            │         ║
║  │            │               │   corpus_search 5 · live_lookup 3 · fetch_paper 8      │         ║
║  │  관찰을 보고│◀──────────────│   read_paper 8 · view_figure 6 · extract_evidence 8    │         ║
║  │  다음 수를 │   새 관찰      │   ★ 상한 초과는 **거부 사유를 관찰에 실어** 되돌린다   │         ║
║  │  고른다    │               └────────────────────────────────────────────────────────┘         ║
║  └─────┬──────┘                                                                                  ║
║        │ 종료 제안                                                                               ║
║        ▼                                                                                         ║
║  ┌────────────────────────────────────────────────────────┐                                      ║
║  │ check_floor   ★ 바닥 1 — 근거가 0건인가                │  미달이면 **사유를 실어 되돌린다**   ║
║  │               ★ 바닥 2 — 주장·비교형인데 반대 측       │  거부만 하면 모델이 같은 제안을      ║
║  │                 탐색(stance=counter)이 0회인가         │  반복하고 예산을 태운다              ║
║  │               ★ 탈출구 — 더 부를 예산이 없으면 통과    │                                      ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼  통과                                                                   ║
║  ┌────────────────────────────────────────────────────────┐  ──▶ [ Bedrock LLM ]                 ║
║  │ answer        게이트를 통과한 **근거만** 보고 판단     │  A1 번호 실재 · A2 숫자 근거         ║
║  │               문장 검사 5종                            │     → 위반 시 **강등**(인용만 잃음)  ║
║  │                                                        │  A3 없는 논문 · A4 인용 0 · A5 종합  ║
║  │                                                        │     비율 50% → **거부**(1회 재생성)  ║
║  └─────────────────────┬──────────────────────────────────┘                                      ║
║                        ▼                                                                         ║
║                       END                                                                        ║
╚══════════════════════════════════════════════════════════════════════════════════════════════════╝

  턴 상한: 반복 12회 · 비용 $0.50 · 정체 판정 600초 · 체크포인트 보존 7일
```

**근거 추출과 답변 사이의 게이트**가 이 모듈의 핵심이다.

```
   추출된 근거 후보
        │
        ▼
   ┌────────────────────────────────────────────────────┐
   │ span 대조    인용문이 원문 블록에 **실재하는가**   │ ★ 없으면 그 근거를 버린다
   ├────────────────────────────────────────────────────┤
   │ 숫자 대조    문장의 숫자가 근거에 있는가           │ ★ number_not_grounded
   └────────────────────────────────────────────────────┘
        │
        ▼  통과한 것만 answer에 들어간다
```

| 왜 | |
|---|---|
| **거부가 아니라 되돌리기** | 종료를 거부만 하면 모델은 같은 제안을 반복하고 그 반복이 예산을 태운다. **사유를 관찰에 실어** 돌려보내면 다음 수가 달라진다 |
| **stance가 프롬프트가 아니라 인자** | "반대 근거도 찾아라"는 당부는 안 지켜지는 것이 실측됐다. 인자로 두면 지켜졌는지가 트레이스에서 **기계로 세어진다** |
| **바닥에 탈출구가 있다** | 예산이 소진된 주장형 턴은 반대 측을 더 찾을 수가 없다. 탈출구가 없으면 종료 제안이 매번 거부되며 남은 반복을 LLM 호출로 태우고 `budget_exhausted`로 끝난다 |
| **강등과 거부를 나눈다** | 번호·숫자가 안 맞는 것은 문장을 남기고 **인용 표시만** 뺀다. 없는 논문이 등장하는 것은 지어낸 것이라 **답변 전체**를 물린다 |
| **판단은 게이트 통과분만 본다** | 떨어진 근거를 보여 주면 모델이 그것을 근거로 쓴다. 게이트가 두 번 열리는 셈이 된다 |

> ┌ **곁다리 — 코퍼스에 없는 논문** ─────────────────────────────────────────────────────────┐
> │ `live_lookup`이 arXiv · Semantic Scholar · OpenAlex를 **동시에** 친다. 소스 하나가 죽어도  │
> │ 나머지로 진행하고 **셋 다 죽었을 때만** 조회 실패다. 결과는 **초록까지**이고 본문은 승격이 │
> │ 담당한다 — 배포본에는 승격 워커가 없으므로 코퍼스 밖 논문은 초록 근거로만 답한다.          │
> │                                                                                            │
> │ 죽은 소스는 결과값으로 날라 모델 관찰과 화면 양쪽에 밝힌다 — **소스가 죽어 못 찾은 것과    │
> │ 세상에 없는 것은 사용자가 할 일이 정반대**다. 소스 이름은 안 싣는다(SEC-9).                 │
> └────────────────────────────────────────────────────────────────────────────────────────────┘

---

## 7 · 검증 층

네 모듈이 같은 방식으로 검사된다 — **domain에 I/O가 없어서** 가능한 구조다.

| | 무엇을 재나 | 비용 |
|---|---|---|
| 단위 · PBT | 게이트 · 랭킹 · 청킹 · 루프 라우팅 | 0 |
| **1층 골든셋**(U11) | 51문항 — 배선과 불변식. 녹화 대역이라 **CI에서 LLM 0회** | 0 |
| **2층 심판**(U11) | 실 스택·실 코퍼스로 문항마다 턴 1회 + 심판 1회 | Bedrock 호출 |
| 실스택 스모크 | 배포본에서 손으로 — 편수 · 전문 · 그림 · 턴 1회 | 실비용 |

**1층 수치를 품질로 읽으면 안 된다.** 대본이 기대대로 움직이므로 recall@k가 **구조적으로**
1.00이다. 재는 것은 배관이고, 1.00에서 떨어지면 품질이 아니라 배선이 깨진 것이다. 품질은
2층이 본다 — 같은 지표가 실 코퍼스에서는 **0.556**이었다(3,248편이 실제로 답할 수 있는 범위).
