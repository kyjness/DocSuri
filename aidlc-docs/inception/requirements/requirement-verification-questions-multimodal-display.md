# 멀티모달 표시(그림·도표) — 요구사항 명확화 질문 (Requirement Verification — Multimodal Display)

**단계**: INCEPTION → Requirements Analysis 재진입 (기능 개정) · **일자**: 2026-06-22
**대상 기능**: 정규화 시 그림·도표 **자산 추출·저장** + 상세보기/전문뷰어 **표시**. 요약/번역 입력은 텍스트+캡션 유지 — **비전(이미지) 추론은 차기 사이클**(스코핑 확정).
**영향 유닛**: U1(인제스천 — 자산 추출·저장) · 공유 계약(`shared/dtos`) · U7(요약/번역 — 통과) · U5(프론트 — 렌더). **다음 FR 번호**: FR-17~
**근거 SSOT**: `requirements.md`(FR-12~14·§12·NFR-FP3·QT-5·SEC-9/12·BR-SF-11/12) · `aidlc-state.md`(보류 트랙 "그림·도표=멀티모달=요구사항 개정") · `summarization-translation-pipeline.md`
**답변 상태**: ✅ 확정 (2026-06-22) — **Q2=C(혼합 추출), 나머지 전부 A(권장안)**. 사용자 승인. 아래 분석 후 `requirements.md` 등재 진행.

> ⚠️ 현 `requirements.md` **§12는 "그림·도표(멀티모달)"를 v1 범위 제외**로 둔다. 본 개정은 그 제외를 **부분 해제**(표시 전용 편입, 비전 추론은 제외 유지)하는 승인 게이트다. 아래 답을 확정해야 `requirements.md`에 FR-17~ 및 관련 개정을 등재한다.
> 각 질문의 **[Answer]:** 에 AI 권장안을 미리 적어두었다. 동의하면 그대로, 바꾸려면 letter를 교체하거나 **X) 기타**로 적어주세요. 특히 **Q2(추출 방식)·Q3(서빙)** 가 실질 갈림길이다.

---

## Q1. 범위 편입 & 단계 (vs §12 제외) — 게이트 본체

그림·도표를 v1.x에 **표시 전용**으로 편입하는가? (자산을 추출·저장해 상세보기/전문뷰어에 보여주되, AI가 이미지를 읽어 요약/근거에 반영하는 비전 추론은 차기 사이클로 분리.)

- **A) 표시 전용으로 편입 — 비전 추론은 제외 유지** (AI 권장): §12에서 "그림·도표" 전면 제외를 **"비전 추론만 제외"로 한정**. 자산 추출·저장·렌더는 FR-17로 편입.

- **B) 편입하되 로드맵에만 기록** — 요구사항만 적고 Construction은 보류.

- **C) 편입하지 않는다** — 현 텍스트+캡션 유지.

- **X) 기타 (please describe after [Answer]: tag below)**

[Answer]: A — 표시 전용 편입. §12 "그림·도표" 제외를 "비전 추론만 제외"로 한정, 자산 추출·저장·렌더를 FR-17로 등재.

---

## Q2. 자산 추출 방식 — **실질 갈림길**

정규화 단계(U1)에서 그림·도표 **이미지 자산**을 무엇에서 뽑는가? (현재는 캡션 텍스트만 보존, 자산 미추출.)

- **A) PDF 페이지 렌더 → 영역(bbox) 크롭** (AI 권장): 레이아웃 분석으로 그림/표 영역을 검출해 페이지 이미지에서 크롭. 소스 형식 무관하게 **견고**, v1 표시 전용에 충분. 단 영역 검출 품질 의존.

- **B) arXiv LaTeX 소스에서 구조화 추출**: `\includegraphics` 그림 파일·`table` 환경 직접 추출 → 원본 화질·구조 우수. 단 소스 미제공 논문은 누락, 빌드 복잡.

- **C) 혼합(소스 있으면 B, 없으면 A 폴백)**:\ 품질·커버리지 최상이나 파이프라인 2경로 유지 비용.

- **X) 기타 (please describe after [Answer]: tag below)**

[Answer]: C — 혼합(arXiv 소스 있으면 LaTeX 구조화 추출 B, 없으면 PDF 페이지 크롭 A로 폴백). 화질·커버리지 최상; 인제스천 2경로 유지 비용은 수용. NFR/Functional Design에서 소스 가용성 판정·폴백 기준 확정.

---

## Q3. 자산 저장·서빙 보안 — **실질 갈림길**

추출 자산을 어디에 저장하고 클라이언트에 어떻게 내보내는가? (SEC-9 내부필드 비노출·SEC-12 토큰 비노출.)

- **A) S3 저장 + 단기 만료 서명 URL** (AI 권장): 앱이 짧은 만료의 서명 URL을 발급해 카드/뷰어가 직접 로드. CDN·캐시 친화적(모바일 성능), 토큰 비노출.

- **B) S3 저장 + BFF 프록시**: 모든 이미지 요청을 앱 서버 경유. 만료·접근제어 단순하나 대역폭·지연 부담.

- **C) 공개 읽기 버킷**: 최단 구현이나 라이선스·접근제어상 부적합.

- **X) 기타 (please describe after [Answer]: tag below)**

[Answer]: A — S3 + 단기 만료 서명 URL(성능·토큰 비노출). 접근제어가 더 필요하면 B로 조정.

---

## Q4. 라이선스 게이트 — 자산에도 적용?

전문 뷰어는 이미 **OA 라이선스 미허용 시 비표시 + arXiv 링크아웃**(BR-SF-11)으로 게이트된다. 그림·도표 자산도 동일하게 가는가?

- **A) 동일 게이트 재사용** (AI 권장): OA 허용 논문만 자산 표시, 비허용은 자산 미노출 + arXiv 링크아웃. 전문 게이트와 한 정책.

- **B) 그림 재배포 별도 라이선스 검토** — 자산 단위 별도 판정(복잡·범위 확대).

- **X) 기타 (please describe after [Answer]: tag below)**

[Answer]: A — 전문과 동일 OA 게이트 재사용. 비허용 논문은 자산 미노출·링크아웃.

---

## Q5. 앵커(출처 보기) 연동 — 이번 단계 범위

요약 응답의 `AnchorVM.target = figure|table`(이미 계약에 예비됨)을 표시 전용 단계에서 어떻게 다루는가?

- **A) 자산 참조로 연결** (AI 권장): "출처 보기 → 해당 그림/표로 스크롤·하이라이트". 표시 전용이라도 텍스트 앵커를 자산에 연결.

- **B) 자산만 렌더, 앵커 연동은 비전 사이클로 미룸**: 이번엔 그림/표를 보여주기만.

- **X) 기타 (please describe after [Answer]: tag below)**

[Answer]: A — figure/table 앵커를 자산 참조로 연결(출처 보기→해당 도표). 단순 텍스트 span 매칭에서 자산 매칭으로 전환.

---

## Q6. 영향 범위 & 기존 정합 갭 흡수

본 개정은 U1·공유계약·U7·U5를 가로지른다. 이때 U7 계약을 재설계하므로, 현존 **백/프론트 정합 갭 3건**을 같은 작업에 흡수할지: ① U7 요약 계약 SSOT 부재(수기 큐레이션 → `summarization.schema.json` 정식 수립), ② `validation_error`/`unauthorized` 상태가 프론트 응답 유니온·분기에 미반영, ③ full-text `unauthorized` 동일.

- **A) 흡수 — 본 개정의 U7 Construction에서 함께 정리** (AI 권장): 계약을 어차피 재생성하니 SSOT 수립 + 상태 매핑을 한 번에. 분기는 새로 파서(본 브랜치) 처리.

- **B) 별도 처리** — 갭 3건은 분리된 작업으로.

- **X) 기타 (please describe after [Answer]: tag below)**

[Answer]: A — 흡수. SSOT(`summarization.schema.json`) 수립 + 상태 매핑(②③)을 U7 Construction에서 함께. 본 브랜치에서 진행.

---

## Q7. 성능(NFR) 프레이밍 — 이미지 렌더

자산 렌더는 기존 **NFR-FP3(대용량 전문 점진/가상화)** 에 더해 이미지 로딩 비용을 더한다(모바일 우선).

- **A) NFR-FP 보강 — 이미지 lazy-load·플레이스홀더·치수 예약** (AI 권장): 가상화와 공존, 레이아웃 시프트·메인스레드 블로킹 회피. 검색 SLA(NFR-P1) 무관.

- **B) 별도 신규 NFR 신설** — 이미지 전용 성능 지표를 따로.

- **X) 기타 (please describe after [Answer]: tag below)**

[Answer]: A — NFR-FP 보강(이미지 lazy-load·플레이스홀더·치수 예약), 검색 SLA 무관.

---

> 답변 확정 후 모호성/모순을 점검하고, 없으면 `requirements.md`에 **FR-17(그림·도표 자산 추출·표시) 등재 + FR-12 앵커(figure/table) 개정 + §12 제외 한정(비전 추론만) + NFR-FP 보강 + QT 보강 + §13 추적성 행** 추가(승인 게이트). 이후 Construction을 U1 → 공유계약 → U7(+갭 3건 흡수) → U5 순으로 진행.

---

## 답변 분석 (모호성·모순 점검) — 2026-06-22

전부 권장안 A + **Q2=C(혼합 추출)**. 내적 정합 확인, 모순 없음. `requirements.md` 등재 시 **반영 메모**:

- **Q1+Q4 정합**: 표시 전용 편입(Q1)이라 비전 LLM·grounding 재설계 없음 → 자산은 OA 라이선스 게이트(Q4)만 통과하면 표시. C-2(생성 글쓰기)·FR-5 근거화 경계 불변.
- **Q2=C 파급 (요구사항 레벨 메모, 수치는 설계 단계)**: 인제스천이 **2경로**(LaTeX 구조화 / PDF 크롭 폴백)가 된다 → U1 Functional/NFR Design에서 ① arXiv 소스 가용성 판정 규칙, ② 폴백 트리거, ③ 자산 화질·바운딩 정규화 기준을 확정. 요구사항 본문은 "그림·도표 자산을 추출·저장한다(추출 방식은 소스 가용성에 따른 혼합)"로 방식 비종속 기술.
- **Q3=A(서명 URL)**: SEC-9/SEC-12 — 자산 URL은 단기 만료 서명, 내부 키·토큰 비노출. Infrastructure Design에 S3 자산 버킷·서명 정책 라인 추가.
- **Q5=A**: `AnchorVM.target=figure|table`(기존 계약 예비)을 자산 참조로 연결 → FR-12 인수기준의 앵커 문구를 "텍스트 span 또는 자산 참조"로 일반화.
- **Q6=A**: 본 개정의 U7 Construction에서 ① `summarization.schema.json` SSOT 수립(수기 큐레이션 종료), ②③ `validation_error`/`unauthorized` 상태를 응답 유니온·분기에 반영. 별도 트랙 아님.
- **Q7=A**: NFR-FP(프론트 성능)에 이미지 lazy-load·플레이스홀더·치수 예약 보강. 검색 SLA(NFR-P1) 무관.
- **잔여(설계 단계 확정)**: 추출 라이브러리·bbox 검출 임계·자산 키 규약·서명 만료 시간·이미지 포맷/해상도 등은 Functional/NFR Design 산출물(요구사항 단계 아님).

**상태**: 모순 없음 → `requirements.md`에 FR-17 등 등재 진행(아래 등재 후 Construction 게이트로).
