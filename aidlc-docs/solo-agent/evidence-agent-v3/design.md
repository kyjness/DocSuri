# evidence 에이전트 v3 — 설계

**단계**: 로드맵 ⑩-1 (두 에이전트 완성 — evidence)
**일자**: 2026-08-21 착수
**상태**: ✅ 초안 확정(2026-08-21) — §1·§2 사용자 확정, §3~§10 결정 5건·기억 범위·평가 방식 확인. 조정 대상 수치: 종합 상한 50%·재생성 횟수·기억 예산 8천 토큰
**전제**: 목적은 `solo-roadmap.md` ⑩-1에서 확정(2026-08-20). 구조·프레임워크는 아키텍처 질문지 Q1=B·Q5=B(2026-08-21). 기존 v2 설계 세트(`../evidence-agent-v2/`)는 **현행 코드의 기술**이며, 이 문서와 다른 곳은 이 문서가 이긴다.
**읽는 법**: §2는 사용자가 화면에서 보는 것만 적는다 — 코드를 몰라도 판단할 수 있어야 한다. §3 이후는 §2를 만들기 위한 내부 설계이고, 각 결정에 이유를 단다.
**실행 계획**: docs PR(이 문서) → PR 1 루프를 LangGraph로 옮김(동작 변화 0) → PR 2 evidence 완성(§2~§8 전부).

---

## 1. 목적·범위 (확정)

> **evidence는 문헌에 근거해 답하는 챗봇이다.**
> 사용자가 던진 주장이나 질문에 대해, 논문을 찾아 읽고, 인용을 달아, **판단까지 포함한 답변**을 한다.
> 근거를 못 찾으면 답을 지어내지 않고 못 찾았다고 말한다.

- 판단은 **조건부**로 한다 — 문헌이 갈리면 갈린 채로 보여주되 무엇이 가르는지를 밝힌다. 종합해 한쪽으로 단정하지 않는다.
- 못 찾으면 **부분 답 + 다음 행동**을 준다.
- 대화 안에서 끝난다 — 침묵 금지 · 언제든 취소 · 취소해도 부분 결과.

**범위 밖**: 논문 추천·요약 자체(U7), 새 연구 방향 제안(novelty), 논문 외 출처(웹·뉴스). 이런 요청은 §2.4의 방식으로 안내한다.

**불변 제약** (v2에서 승계): C-2 날조 금지 — 인용·수치는 기계식으로 검증되고 LLM-judge를 쓰지 않는다. 비용 판단은 U6 `get_budget_state()`만 한다.

---

## 2. 대화 동작 — 사용자가 보는 것 (2026-08-21 확정)

### 2.1 답변 모양 — 산문 판단 + 근거 목록

채팅에 **대화하듯 산문으로 판단**이 먼저 오고, 그 아래 **근거 목록**(논문 대조, BR-EV-5 승계)이 붙는다. 구획 라벨은 쓰지 않는다.

```
데이터가 적고 도메인이 사전학습과 가까울 때는 LoRA가 비슷하거나 더 나은
성능을 내요[1][2]. 반면 도메인이 크게 다르거나 학습량이 클 때는 전체
파인튜닝이 앞서요[3]. 갈리는 지점은 '적응 과제가 사전학습 분포 안에
있느냐'예요.

⚡ 쟁점
  [2] 도메인 차이가 크면 전체 파인튜닝이 앞선다
      ✓ LoRA Learns Less and Forgets Less ↗   § 5.1
        "full fine-tuning learns more and forgets more"
      ✗ LoRA: Low-Rank Adaptation… ↗          § 4.2
        "LoRA performs on-par or better than fine-tuning"

근거
■ LoRA: Low-Rank Adaptation of Large Language Models ↗           7건
  [1] ✓ § 4.2   "we reduce the number of trainable parameters by 10,000 times"
  [3] ✓ 표 1     GLUE 평균 86.4를 더 적은 파라미터로 따라잡는다
  [5] ✓ § 5.1   "no additional inference latency"
  ▾ 4건 더 보기
■ QLoRA: Efficient Finetuning of Quantized LLMs ↗   [초록]        3건
  [4] ✓ "4-bit quantization preserves full 16-bit performance"
```

**논문이 블록인 이유.** 표가 값을 하는 것은 논문이 여럿이고 서로 엇갈릴 때다. 실제로 가장 흔한 턴은 한두 편이라(실측: 논문 2편에 근거 10건) 같은 제목이 근거마다 반복돼 화면이 이름으로 덮인다. 폰에서는 3열 격자의 가로 스크롤 안에 인용문까지 들어가 읽히지 않았다.

**줄에는 원문을 쓴다.** 한국어 명제는 바로 위 판단 산문이 거의 그대로 말하므로, 둘 다 두면 같은 사실이 **세 번** 나온다(산문 → 명제 → 인용문). 다만 **표·그림·식 인용은 명제로 간다** — 그쪽 원문은 `PPL | 8.08 | 11.44 | …` 같은 셀 덤프라(실측: 출처의 약 15%) 명제 없이는 읽히지 않는다. 판정은 `anchorType` 하나로 기계가 한다.

**상충 근거는 목록 맨 위 `쟁점`으로 빼낸다.** 논문으로 묶으면 "한 논문이 지지하고 다른 논문이 상충하는" 근거가 두 블록으로 갈라져 엇갈림을 한자리에서 못 본다 — 표가 있던 이유가 정확히 그것이다(BR-EV-5). 빼내면 `[n]`의 DOM id 중복도 안 생긴다. 쟁점에서만 논문 이름이 줄마다 나온다(그 자리는 어느 쪽이 무엇을 말했는지가 줄에 있어야 한다).

**인용 위치는 누르면 그 블록으로 간다.** 논문 상세가 본문 뷰어를 여는 것과 같은 경로다(`/paper/{id}/doc-model?version=&anchorId=`). 둘은 링크를 안 만든다 — 코퍼스 밖 논문(우리 doc-model이 없다)과 초록 앵커(`s0.*`, 본문 뷰어가 그 섹션을 목록에서 빼므로 스크롤이 **조용히** 안 된다 — 초록이 보이는 상세로 보낸다).

**출처는 제목이 이름이다.** `arxiv:2106.09685v2` 같은 식별자를 그대로 찍으면 사용자는 눌러 보기 전까지 무슨 논문인지 모른다. 목적지는 `SourceRef.namespace`가 정한다 — 코퍼스 논문(네임스페이스 없음)은 우리 상세로(본문·요약·번역이 거기 있다), 코퍼스 밖은 arxiv.org·doi.org로, **사용자 업로드 문서는 링크 없음**(실재 arXiv id가 없으므로 URL을 조립하면 그것이 날조다).

**네임스페이스는 파생값인데도 싣는다.** `paperId`에서 잘라낼 수 있지만, 그러면 접두어를 **만드는 곳**(`live_sources`·`user_docmodel`)과 그것을 보고 **목적지를 정하는 곳**(화면)이 각자 문자열을 적게 된다 — 접두어가 하나 늘면 한쪽만 고쳐지고 화면은 조용히 링크를 잃는다. 어휘를 아는 쪽이 판정해서 실어 보내면 소비자는 되파싱하지 않고, 어휘가 늘 때 소비자의 분기가 컴파일에서 막힌다. 다만 **코퍼스 논문을 어디로 보낼지는 화면이 정한다** — `/paper/{id}`는 프론트 라우트라 백엔드가 알 자리가 아니고, 백엔드가 URL을 만들면 외부만 만들고 내부는 그대로라 오히려 두 갈래가 된다.

**논문마다 3건까지 펴 두고 나머지는 접는다.** 근거 건수에 상한이 없어(추출 호출 8회 × 호출당 무제한) 화면을 덮을 수 있는데, 분포가 한쪽으로 쏠린다(실측: 논문 4편 중 하나가 14건, 나머지 셋은 2건씩) — "전체 상위 N건" 규칙은 그 구조에 안 얹힌다. 논문은 전부 보이고 각 논문 안에서만 접는다. 접힌 근거도 **번호와 DOM 위치는 그대로 남는다** — 산문의 `[9]`가 가리킬 대상이 사라지면 링크가 죽는다. 그 번호를 누르면 **그 논문 그룹이** 펴진다.

**판단 문장의 두 종류.** `[n]`이 붙은 문장은 그 근거가 실제로 그 논문에 있음을 기계가 확인한 문장이다. 번호 없는 문장(위의 "갈리는 지점은 …")은 **모델이 근거들을 종합해 쓴 문장**이라 기계가 확인할 수 없다. 화면에서 구분한다 — 숨기지도, 같은 급으로 보이게 하지도 않는다. 구분 방식(배지·색)은 §8.

### 2.2 문헌이 갈릴 때

한쪽으로 단정하지 않는다. **어떤 조건에서 어느 쪽인지**를 말하고, 갈림 지점을 한 문장으로 밝힌다.

```
○  "데이터가 적을 때는 LoRA[1][2], 도메인이 다를 때는 전체 파인튜닝[3]. 갈리는 지점은 ~."
✗  "대체로 LoRA가 유리합니다."      ← 근거로 확인할 수 없는 단정
✗  "논문마다 다릅니다."             ← 판단을 안 한 것
```

근거 목록에서 상충 명제는 논문 그룹 위의 `쟁점`으로 빠지고, 그 안에서 지지 `✓`와 상충 `✗`가 나란히 선다(BR-EV-5).

### 2.3 근거가 부족하거나 없을 때

**대화가 끊기지 않는다.** 찾은 만큼 답하고, **탐색이 잘렸을 때만** 어디까지 봤는지 한 줄 붙이고, 다음 수는 산문으로 제안한다. 버튼은 없다 — 사용자가 타이핑으로 이어간다.

```
[부분 — 근거는 있으나 한쪽만]
  데이터가 적을 때 LoRA가 유리하다는 근거는 있어요[1]. 반대 조건(도메인이 크게
  다를 때)을 다룬 논문은 확인한 범위에서 찾지 못했어요.
  후보 24편 중 8편까지 보고 멈췄어요 — 이어서 더 찾아볼까요?
  (근거 1건)

[없음 — 후보는 있었음]
  이 질문에 대한 근거를 확인한 논문에서 찾지 못했어요. 후보 12편을 모두 봤어요.
  질문을 다른 표현으로 바꾸거나, 범위를 넓혀 다시 물어봐 주세요.

[없음 — 후보조차 없음]
  이 주제를 다룬 논문을 찾지 못했어요. 주제를 넓히거나 다른 용어로 물어봐 주세요.
```

"확인 범위" 한 줄은 **예산·취소·실패로 잘렸을 때만** 나온다(v2 Q10 승계). 충분해서 멈춘 정상 답변에는 안 나온다. 무엇을 제안할지는 멈춘 이유에 따른다 — 예산이면 "이어서", 후보가 없으면 "넓히기", 후보는 있는데 근거가 없으면 "다른 표현".

### 2.4 문헌으로 답할 수 없는 질문

잡담·코드 작성·개인 의견·논문 외 출처 — 검색을 시작하지 않고 바로 답한다.

```
Q. 오늘 뭐 먹을까?
   저는 논문에서 근거를 찾아 답하는 도우미예요. 이런 질문을 해보세요 —
   "LoRA가 전체 파인튜닝보다 좋아?" / "RAG가 긴 문맥 모델보다 정확해?"
```

판정은 루프 안의 모델이 한다(별도 분류기 없음 — novelty 세션 메모리 때와 같은 원칙). 단 **검색을 한 번도 안 했을 때만** 허용된다 — 검색 뒤의 "범위 밖"은 §2.3의 "없음"으로 처리한다.

### 2.5 후속 질문

같은 세션의 이전 대화를 기억한다. **턴 수가 아니라 토큰 예산으로** 자른다 — 최근 턴은 약 8천 토큰까지 그대로(질문·답변 요약·인용 논문, 턴당 약 400토큰이라 약 20턴), 넘치면 오래된 것부터 한 단락 요약으로 접는다. **인용한 논문 id는 세션 전체를 유지**한다(짧아서 부담이 없고, 후속 질문이 그 목록 위에서 돈다). 8천은 시작값이며 실측 후 조정한다.

후속 질문은 그 위에서 좁히거나 넓힌다.

```
턴 1  "LoRA가 전체 파인튜닝보다 좋아?"   → 논문 A B C 인용
턴 2  "그중에서 2023년 이후만"           → A B C 중 2023+ 만 다시 봄, 새 검색 없음
턴 3  "이어서 더 찾아줘"                 → 같은 질문, 아직 안 본 후보부터
턴 4  "QLoRA는?"                        → 새 주제. 새로 탐색하되 A B C는 후보에 남김
```

### 2.6 논문을 찾는 곳 — 코퍼스 먼저, 없으면 같은 원천에서 실시간 조회

코퍼스는 ingestion이 **arXiv·S2·OpenAlex**에서 수집해 둔 논문이다. 원래 목표(세 원천 약 10년치)대로 수집됐으면 코퍼스만으로 충분하지만, 지금은 기반 논문 약 4,000편이라 **코퍼스에 없는 논문을 같은 세 원천에서 질문 시점에 가져온다.** 이것은 "외부 검색"이 아니라 **실시간 조회**다 — 원천이 같다.

```
1. 코퍼스 검색 (하이브리드, U2 재사용)
2. 부족하면 → arXiv · S2 · OpenAlex 실시간 조회 (초록만 옴)
3. 본문이 필요하면 → 그 자리에서 가져와 파싱(온디맨드 승격, v2 Q5=B)
4. 실시간으로 찾은 논문은 백그라운드로 색인 → 쓸수록 코퍼스가 그쪽으로 자란다
```

- 실시간 조회는 지금 arXiv API 하나인데 **세 원천으로 맞춘다.** 수집하는 곳과 찾는 곳이 같아야 "시스템이 알 수 있는 것 = 찾을 수 있는 것"이 된다. 클라이언트·ID 정규화·중복 제거는 ingestion 것을 재사용한다.
- 코퍼스가 목표치에 가까워져도 설계는 바뀌지 않는다 — 실시간 조회를 부르는 횟수만 줄어든다.
- **논문 밖 출처(GitHub·데이터셋)는 찾지 않는다.** 검증이 안 돼서가 아니라, 그 출처가 이 에이전트의 질문("문헌상 이 주장이 맞나")에 답하지 않기 때문이다. "구현체가 있나"는 novelty의 질문이다.

### 2.7 진행 중

누르자마자 **수락** 신호가 오고, 그 뒤 도구를 부를 때마다 한 줄씩 쌓인다. 취소는 처음부터 끝까지 가능하다.

```
● 코퍼스 검색 "LoRA full fine-tuning comparison" → 24편 (제목·초록)
● 본문 확보 — Hu 2021, Dettmers 2023
● 본문 읽는 중 — Hu 2021 (문단·표·수식 87블록)
● 그림 보는 중 — Hu 2021 Figure 2
● 근거 추출 — 3건 확보 (2건 탈락)
● 실시간 조회 (S2) → 6편
● 본문 확보 — Biderman 2024
                                              [취소]
```

검색 결과에 제목·초록이 같이 오므로 "초록 확인"은 별도 단계가 아니다. 모델이 읽을 논문을 고르면 **DocModel 전 블록**(문단·리스트·표·그림 캡션·수식·알고리즘, Q1=A)을 읽고, 그림·수식 자체가 필요하면 비전으로 본다(`view_figure`).

활동 줄에는 **논문 제목·건수·단계**만 싣고 인용문은 싣지 않는다 — 검증 전 문장이 화면에 나오면 안 된다(C-2).

### 2.8 취소

취소하면 **지금 하던 한 단계가 끝나는 대로** 멈춘다(보통 수 초). 그 시점까지 검증된 근거로 §2.3의 부분 답을 만든다 — "취소됨 · 후보 24편 중 5편 확인"이 확인 범위 한 줄로 붙는다. 멈추는 사이 실행된 도구의 결과도 부분 답에 들어간다.

### 2.9 비용 한도

- **시작 전**에 한도면: "오늘 이용량에 도달했어요. 내일 00:00에 다시 열려요." 이전 답 다시 보기는 된다.
- **도중**에 한도면: 취소와 같은 경로 — 부분 답 + "내일 이어서 찾을 수 있어요".

"제한" "degraded" 같은 내부 용어는 쓰지 않는다(v2 Q10 승계).

---

## 3. 루프·도구

### 3.1 그래프 (LangGraph, Q5=B)

한 턴은 LangGraph `StateGraph` 하나다. 상태는 현행 `LoopState`(확보 논문·후보·누적 근거·트레이스·노트)에 §3.3의 필드를 더한 것이고, 체크포인트는 **Postgres saver**(이미 있는 DB)에 super-step마다 남는다. `thread_id = turn_id`.

```
          ┌─────────────────────────────────────────────┐
          ▼                                             │
 [decide] ── 도구 호출 ──▶ [act] ── 결과를 관찰에 ───────┘
    │
    ├─ 종료 제안 ──▶ [check_floor]  바닥 미달이면 사유를 노트에 싣고 decide로
    │                    │ 통과
    │                    ▼
    │                [answer]  검증된 근거만 보고 산문 판단을 쓴다 (§4)
    │                    ▼
    │               [assemble]  근거 목록·확인 범위·답변 검사 → 결과
    │
    └─ 범위 밖 제안 ──▶ [check_scope]  검색 0회일 때만 통과 → 안내문으로 종료 (§2.4)

 예산 소진 / 취소 / 치명 오류 ──▶ [answer](근거 있으면) ──▶ [assemble]
```

- **노드는 평범한 파이썬 함수**다. `decide`는 현행 Bedrock 어댑터를 그대로 부르고, `act`는 현행 `ToolRegistry`를 그대로 쓴다 — 이 이전에서 LLM 연결은 손대지 않는다. (LangChain 도입 여부는 이 문서가 정하는 것이 아니라 미결이다 — 질문지 Q5 정정 블록 참조.)
- 현행 `run_loop()`의 불변 조건은 그대로 노드에 산다 — 예산 검사는 act 직전 1회, 도구 호출은 `ToolCallRecord` 1:1, 이미지는 decide 직후 소비.
- **PR 1**은 이 그래프로 옮기되 `answer` 노드 없이(현행 `_narrative`), `check_floor`는 현행 규칙(근거 0건 거부)만으로 만든다 — 동작 변화 0을 증명한 뒤 PR 2에서 나머지를 얹는다.
- **PR 1 실행 결과(2026-08-23, `feature/evidence-langgraph-loop`)**: 노드 3개(`decide`/`check_floor`/`act`), `compile()`에 체크포인터 없음 — `LoopState`가 doc_model·이미지를 들고 있어 직렬화가 안 되므로 **Postgres saver·`thread_id=turn_id`는 PR 2**(상태 직렬화와 함께). `finish`/`assemble` 노드도 없다 — runner가 여전히 `assemble()`을 부르고 종료는 `_finish` 4곳이 `outcome`을 채우는 것으로 표현된다; PR 3이 이를 `answer → assemble` 꼬리로 모은다. 스텝 상한은 예산에서 유도: `_STEPS_PER_ITERATION(2)·n + _TAIL_STEPS(1) + 1`(LangGraph 1.2.x는 N스텝에 limit ≥ N+1, 실측). PR 3의 `answer`·`assemble`은 꼬리 스텝이라 `_TAIL_STEPS`를 올린다. 배포는 `langgraph==1.2.11` 정확 고정(상한이 여유 없이 잡혀 있어서). LangSmith 자동 트레이싱은 `tracing_context(enabled=False)`로 차단.
- **PR 2 실행 결과(2026-08-24, `feature/evidence-turn-execution`)**: 그래프 채널은 **순수 JSON**(`snapshot`·`proposal{kind}`·`outcome{reason,detail}`)이고 살아 있는 `LoopState`·deps는 `context_schema=LoopRun`으로 주입된다(체크포인트 밖). Postgres saver가 super-step마다 저장한다(`thread_id=turn_id`, 풀은 autocommit).
  - **스냅샷에는 마감(`assemble`)이 읽는 것만 싣는다.** DocModel·투영 캐시·이미지는 직렬화가 안 되고, 도구 결과 관찰 윈도우·소모 예산·후보 논문의 초록 본문은 되읽는 소비자가 없다. 상태를 **바꾼 노드만** 돌려준다(`act`와 종료 지점) — `decide`·`check_floor`가 만지는 것은 예산·이미지·노트뿐이고, 매 노드가 전체 투영을 다시 쓰면 같은 바이트가 blob과 writes에 두 번씩 쌓인다. 실측: 7도구 턴 체크포인트 16건·**59.7 kB**(초안대로 전부 실었을 때는 9도구 턴에 22건·493 kB였다).
  - **완료된 thread는 다시 invoke해도 아예 돌지 않는다**(입력 쓰기조차 반영되지 않는다 — 1.2.x 실측). 그래서 `run_loop`는 invoke 전에 스레드를 비운다: 재배달된 잡이 "아무 일도 안 하고 성공"으로 끝나는 것을 막는 유일한 방법이다. 재개가 아니라 새 실행이므로 이 줄은 PR 4 이어가기가 먼저 걷어내야 한다고 적었으나 **틀렸다**(PR 4에서 정정): `thread_id = turn_id`라 이 줄은 *현재 턴* 스레드만 지우고 이전 턴 스냅샷은 무사하다. 이어가기를 "이전 턴 스냅샷을 씨앗으로 이식"으로 구현하면 충돌하지 않으므로 이 줄은 그대로 둔다 — 재배달된 잡이 무동작으로 성공하는 것을 막는 유일한 장치이고, 걷어내면 그것을 대체할 것을 새로 만들어야 한다.
  - 그래프는 **`TurnCheckpoints`가 소유**하고 프로세스당 한 번 컴파일한다. 러너는 그 그래프를 받아 돌리기만 하고, 스냅샷 조회·삭제는 체크포인트 객체 책임이다 — 러너에 매달았더니 세션 삭제·취소·이벤트·폴링 5개 라우트가 LLM 러너에 묶여, 러너가 구성되지 않는 배포에서 세션 삭제가 500이 됐다.
  - 취소·중단은 `deps.should_stop`이 `decide` 진입(super-step 경계)에서 사유를 돌려주는 것으로 표현한다 — `RunControl.request_drain()`은 예외(`GraphDrained`)로 빠져 꼬리와 한 길이 아니라 쓰지 않았다. `_finish`는 `FATAL_ERROR`·`CANCELLED`·`INTERRUPTED`를 근거 0건이어도 강등하지 않는다.

- **PR 3 실행 결과(2026-08-24, `feature/evidence-judgment`)**: `answer` 노드를 얹었다. 근거가 있으면 **모든 종료 경로**(SUFFICIENT·BUDGET_EXHAUSTED·CANCELLED·FATAL_ERROR·INTERRUPTED)가 이 노드를 지난다. 그림의 `assemble` 노드는 **만들지 않았다** — 순수 함수라 super-step과 체크포인트 쓰기를 하나 더 쓸 이유가 없고, 고아 마감이 이미 스냅샷에서 같은 `assemble()`을 부른다. `_TAIL_STEPS`는 1 → 2(재생성은 노드 **안에서** 일어나므로 스텝을 더 쓰지 않는다). `question_kind`는 §3.3이 "첫 decide"라고 적었으나 **종료 도구(`finish`)의 인자**로 뒀다 — 읽는 곳이 둘 다 종료 이후(판단 프롬프트·PR 4 바닥 검사)이고, 매 도구 호출마다 반복시키면 값이 턴 안에서 흔들린다. 범위 밖 종료도 첫 턴에 `finish`를 부르는 것으로 표현되므로 선언 시점은 여전히 첫 판단이다. 예산 소진·취소로 끝난 턴은 선언 기회가 없어 `unknown`으로 읽는다.
  - **2층 1회 실행(§10 완료 조건)**: 라벨 문항 6건 · 판정 18개 중 **17 pass / 1 fail**. 1층은 위반 0 · 인용 실재율 1.00 · 폴백 0.00 · 재생성 0.00 · 강등 3건 · recall@k 0.778. 남은 1건은 후속 질문이 이전 턴 논문으로 좁히지 않고 새로 찾은 것 — §3.4 이어가기가 PR 4이므로 **예상된 미달**이다. 실행 중 심판이 잡아 고친 것 하나: 사실형 질문에 조건을 억지로 나누던 프롬프트(규칙 3을 항상 적용했다). 그 문항은 3 fail → 3 pass가 됐다.
  - **첫 실행은 판단이 전부 사라진 채였다**(`fallback_rate=1.0`). 모델이 `{"sentences": …}`가 아니라 맨 JSON 배열로 답하는데 파서가 못 읽어 문장 0건 → A4 거부 → 재생성 → 폴백으로 흘렀다. 예외도 로그도 없었다 — 골든셋이 없었으면 "판단 없는 답"이 그대로 나갔다.
  - **같이 고친 실측 결함**: `_act`가 **도구별 상한**(`tool_cap_exhausted`)을 전역 예산 소진과 똑같이 취급해 턴을 끝내고 있었다. `fetch_paper` 3/3에 막힌 턴이 논문 3편·256블록을 쥔 채 `extract_evidence`를 한 번도 안 부르고 "근거 부족"으로 기권했다(예외도 ERROR 로그도 없다). 상한 거부는 이제 관찰 노트로 돌아가고, `cap_fetch_paper` 기본값은 실측(6회)에 맞춰 3 → 8. 같은 질문이 상한 3에서 `abstain`, 8에서 `ok`·근거 10건이었다.

### 3.2 도구

| 도구 | 현행 대비 | 턴당 상한 |
|---|---|---|
| `corpus_search` | 유지 + **`year_from`·`year_to` 인자 추가** — §2.5의 "2023년 이후만"과 "최근 연구는?"에 답하려면 연도 제약이 프롬프트 당부가 아니라 인자여야 한다 | 5 |
| `live_lookup` | `external_search`의 **이름·범위 변경** — arXiv 하나에서 **arXiv·S2·OpenAlex 셋**으로. 클라이언트·ID 정규화·중복 제거(승자 arXiv > S2 > OA)는 ingestion 것을 재사용한다고 적었으나 **성립하지 않았다**(PR 4 정정): backend는 `docsuri_ingestion`을 의존하지 않고(import 자체가 마운트를 죽인다) 그쪽 S2·OpenAlex 소스는 날짜 창 **수확**용이라 질의 검색 메서드가 없다. 재사용할 대상이 저쪽에 없으므로 evidence 어댑터에 얇게 새로 썼다 — 승자 규칙만 `ingestion/domain/canonical.py`와 두 곳이 된다. 결과는 초록만. 소스별 서킷 브레이커(novelty `SourceBreaker` 공유) | 3 (세 소스 합산) |
| `fetch_paper` | 유지 — 온디맨드 승격(v2 Q5=B), 파싱은 동기·색인은 백그라운드 | 3 |
| `read_paper` | 유지 | 8 |
| `view_figure` | 유지 | 6 |
| `extract_evidence` | 유지 + **`stance` 인자 추가**(`support \| counter \| neutral`) — §3.3의 바닥 검사 재료. 모델이 "반대 근거를 찾는 중"이라고 **선언**하게 해서 시스템이 셀 수 있게 한다 | 8 |

검색 도구 둘(`corpus_search`·`live_lookup`)에도 같은 `stance` 인자를 둔다. 인자는 트레이스에 남으므로 "반대 측을 찾았는가"가 기계적으로 판정된다. 프롬프트 당부("반대 근거도 찾아라")로 두지 않는 이유는 novelty에서 당부가 지켜지지 않는 것을 실측했기 때문이다(루프 미사용 사례). 모델이 stance를 잘못 붙일 수 있으므로 §6 기계 채점에 "반대 측 탐색 여부"를 둔다.

**explicit scope**(사용자가 논문을 지정)는 현행대로 검색 도구 둘을 **등록하지 않는다** — 프롬프트가 아니라 구조로 막는다(BR-EV-2).

### 3.3 종료 조건 — 바닥과 천장

현행은 "근거 0건이면 거부" 하나다. 바닥을 둘로 올린다(2026-08-21 확정).

**바닥** (이 아래로는 정상 종료 불가):
1. 검증 통과 근거 ≥ 1건 (INV-EV-2 승계)
2. 질문이 **주장·비교형**이면 `stance=counter`로 표시된 검색 또는 추출이 ≥ 1회

**천장**: 예산(반복 12 · 도구 30회 · $0.50, 현행 수치 유지 — `answer` 단계 비용도 이 한도에 계상).

그 사이는 모델이 정한다. 바닥 미달 종료 제안은 거부하고 **미달 사유를 노트에 실어** 다음 decide가 달라지게 한다(현행 `_NO_EVIDENCE_NOTE` 방식).

**질문 유형**은 첫 decide에서 모델이 선언한다 — `question_kind: claim | comparison | fact | out_of_scope`. 상태에 저장되고 트레이스에 남는다. `fact`("X는 몇 년에 나왔어")는 반대 측 조건을 면제한다. 별도 분류기는 두지 않는다.

**범위 밖**(`out_of_scope`)은 검색 0회일 때만 종료로 인정한다. 검색을 한 뒤의 범위 밖 제안은 "후보 없음"(§2.3)으로 처리한다 — 비용을 쓰고 나서 안 하겠다고 하는 길을 막는다.

### 3.4 후속 질문과 이어가기

관찰에 싣는 이전 대화는 §2.5의 규칙을 따른다 — 토큰 예산(8천)까지 최근 턴 그대로, 그 이전은 요약 한 단락, 인용 논문 id는 세션 전체. 현행 `_PRIOR_TURNS = 3`은 근거 없이 옮겨온 수치라 폐기한다. 요약은 예산을 넘기는 턴이 생길 때 한 번 만들어 세션에 저장하고(매 턴 재요약하지 않는다), 이후 턴이 밀려날 때마다 그 요약에 덧붙인다. 여기에 둘을 더한다:

- **이어가기**("이어서 더 찾아줘"): 직전 턴의 체크포인트에서 **후보 목록(발견했으나 안 읽은 논문)과 확인한 논문 집합**을 새 턴의 씨앗으로 옮긴다. 검색부터 다시 하지 않는다. 판정은 모델이 하되(`continuation: true` 선언), 씨앗 이식은 기계다 — 라고 적었으나 **선언할 자리가 없다**(PR 4 정정): 이식은 루프가 돌기 전이라 모델은 아직 아무것도 못 봤다. 씨앗은 무조건 심고 관찰의 "확인 대기 논문"에 실어 모델이 쓸지 고르게 한다 — explicit scope에서 이미 그 자리가 그렇게 동작한다. PR 3이 `question_kind`를 첫 decide에서 `finish` 인자로 옮긴 것과 같은 종류의 정정이다.
- **좁히기**("그중에서 2023년 이후만"): 이전 인용 논문 집합을 후보로 올리고 검색 없이 다시 읽는다 — 현행 `prior_paper_ids` 경로에 `year_from`이 붙는 것뿐이다.

---

## 4. 검증 — 게이트와 판단 층의 경계

### 4.1 현행 게이트는 그대로

근거 한 건이 통과하는 6단 검사(논문 실재 · 인용 20자 이상 · 글자 그대로 · 앵커 블록 실재 · 인용이 그 블록 안 · 숫자가 인용문에 있음)는 **한 글자도 바꾸지 않는다.** 이것이 가장 비싼 자산이고, 판단 층은 이 위에 얹힌다.

### 4.2 판단 층 — `answer` 노드

입력은 **게이트를 통과한 근거만**이다. 번호를 붙여 준다.

```
질문: LoRA가 전체 파인튜닝보다 좋아?
질문 유형: comparison

근거:
[1] (Hu 2021, §4.2 표2) "LoRA ... 0.01% ... matches"  — 명제: LoRA는 파라미터 0.01%로 파인튜닝 성능에 도달
[2] (Dettmers 2023, §5) ...
[3] (Biderman 2024, §6.1) ...  ↔ [1]과 상충

규칙:
- 문장마다 근거 번호를 단다. 번호 없는 문장은 종합이다 — 필요할 때만 쓴다.
- 위 근거에 없는 논문·수치를 쓰지 않는다.
- 갈리면 조건을 나눠 말하고, 갈림 지점을 한 문장으로.
- 단정하지 않는다.
- 문장마다 role을 붙인다: conclusion(답 그 자체, 맨 앞에 하나) · evidence(받치는 서술) ·
  divergence(갈림 지점). 갈리지 않으면 divergence를 쓰지 않는다.
```

출력은 문장 단위 구조다 — 산문 한 덩어리가 아니라 `{text, refs[], role}`의 목록. 이 구조가 §4.3 검사와 §8 렌더의 계약이다.

**`role`은 `kind`와 다른 축이다.** `kind`(cited/synthesis)는 "기계가 확인했는가"이고 **도메인이 `refs`에서 유도한다** — 모델에게 스스로 선언하게 두면 그 선언을 또 검사해야 하고 판정 지점이 둘이 된다. `role`은 "문장이 무엇을 하는가"이고 **모델이 선언한다** — refs에서 유도할 수 없고, 그것을 아는 것은 그 문장을 쓴 쪽뿐이다.

**역할은 검사하지 않는다.** §4.3 검사 5종은 role을 보지 않고, 강등도 role을 지우지 않는다 — 강등된 결론은 여전히 결론이고 화면은 그것을 앞에 세우되 '종합' 배지를 함께 붙인다. 여기서 역할을 지우면 강등된 답변만 문단 구조를 잃는다. 선언이 없거나 어휘 밖이면 `evidence`로 읽어 산문이 종전과 같이 평평하게 나간다 — 구조를 못 얻을 뿐 문장이 사라지지 않는다.

**Q3=B로 배제한 `confidence`와 혼동하지 말 것.** 그쪽은 모델이 근거의 세기를 스스로 매기는 값이라 배제했다. role은 세기가 아니라 문장의 자리이고, 틀려도 문단 순서가 어색해질 뿐 근거의 강도를 날조하지 않는다.

### 4.3 판단 문장 검사 — 기계식, LLM-judge 없음

게이트가 근거를 검사하듯 **답변도 기계가 검사**한다. 전부 문자열·집합 연산이다.

| # | 검사 | 실패 시 |
|---|---|---|
| A1 | `refs`의 모든 번호가 실제 근거 번호인가 | 그 문장을 **종합으로 강등** (번호 제거) |
| A2 | 번호가 붙은 문장의 **숫자**가 참조한 근거의 statement·quote에 있는가 (게이트의 `_NUMBER` 풀 재사용) | 그 문장을 종합으로 강등 |
| A3 | 답변에 **근거 목록에 없는 논문**(id·제목)이 등장하는가 | 답변 **거부** |
| A4 | 번호 붙은 문장이 **0개**인가 | 답변 거부 |
| A5 | 종합 문장 비율이 상한을 넘는가 — **시작값 50%**(아래 실측) | 답변 거부 |

**A5 상한 실측(2026-08-24, 골든셋 6문항)**: 문항별 종합 비율은 0.00~0.33, 평균 **0.193**이었고 상한 50%가 **한 번도 물지 않았다**. n=6으로는 조이지 않는다 — 표본이 작을 때 상한을 실측 최대치에 붙이면 다음 문항 하나에 거부가 터진다. 골든셋이 커지면 다시 본다.

처음 적은 값은 0.11~0.33 · 평균 0.232였다. `summarise`가 평균을 낼 때 진리값으로 걸러 **0.0인 문항 하나가 통째로 빠져** 있었다 — 하필 종합 문장이 하나도 없는 최선의 경우다. 지표를 재는 코드도 지표다.

거부되면 사유를 실어 **재생성**한다(시작값 1회, 실측 후 조정 · 비용은 예산에 계상). 재생성도 거부되면 **현행 `_narrative()`(결정론 이어붙이기)로 떨어진다** — 답은 나가되 판단 없이. C-2 fail-closed가 여기서도 성립한다: 검사를 못 통과한 판단은 화면에 가지 않는다.

강등(A1·A2)은 거부가 아니다 — 문장은 남되 "기계가 확인함" 표시를 잃는다. 거부 사유·강등 건수는 트레이스에 남고 §6 지표가 된다.

### 4.4 계약 변경

`EvidenceResult.answer`가 문자열에서 구조로 바뀐다:

```
answer: {
  segments: [ { text, refs: [n...], kind: "cited" | "synthesis",
                role?: "conclusion" | "evidence" | "divergence" } ... ],
  checks:   { demoted: n, regenerated: bool, fallback: bool }
}
```

`claims`·`coverage`는 그대로다 — 결정론 부분은 손대지 않는다. 근거 목록의 항목은 `claims`에서, `[n]` 번호는 `claims`의 순서에서 나온다.

**결정론 전제의 재배치**: `assemble()`은 "같은 입력이면 같은 출력"이었다. 이제 `answer`는 LLM 출력이라 그렇지 않다. 회귀 픽스처·PBT는 **`claims`·`coverage`·§4.3 검사기**에 건다(검사기는 결정론이다 — 같은 답변·같은 근거면 같은 판정). `answer` 품질은 §6 골든셋이 본다.

---

## 5. 실행 — 취소·진행·진입 경로

### 5.1 진입 경로를 하나로

현행은 셋이다 — 동기 JSON(끝날 때까지 침묵) / 동기 SSE / SQS 잡(폴링만). 목적("침묵 금지 · 언제든 취소")을 셋 다 만족시키려면 셋을 따로 고쳐야 하므로 **하나로 합친다**:

```
POST /api/evidence/turns      → 즉시 { sessionId, turnId }  (수락)
GET  /api/evidence/turns/{id}/events   SSE — 진행 · 부분 결과 · 완료
POST /api/evidence/turns/{id}/cancel   취소
GET  /api/evidence/turns/{id}          폴링 폴백 (SSE 끊겼을 때 복구 좌표)
```

- 턴은 **항상 백그라운드 실행**이다(LangGraph thread + Postgres 체크포인트). 실행자가 같은 프로세스의 태스크인지 SQS 워커인지는 배포 환경이 정하고 **API·이벤트 계약은 같다.** 동기 경로는 없어진다 — 수 초 걸리는 턴도 같은 길로 간다(수락 → 이벤트 → 완료).
- SSE 이벤트 형식은 novelty와 같은 `progress` 프레이밍을 유지한다(FE 파서 공유).
- **PR 2 실행 결과**: 실행자 둘(SQS 워커 `worker.py` · 프로세스 내 스레드풀 `executor.py`, SQS 미구성이면 후자)이 같은 `process_job`을 돈다. 실행 전 관문(소유권·존재·멱등)은 `_prepare_turn` 하나이고, 짧은 트랜잭션 틀은 `repository.in_transaction` 하나다 — 네 곳이 각자 베껴 쓰다 한 곳에서 롤백이 빠져 있었다. `GET /jobs/{jobId}`와 `job_id` 컬럼은 없어졌다 — 폴링·이벤트·취소가 전부 `turn_id`다. 수락은 202이고 첨부 해석(PDF 빌드 대기)은 수락 **앞**에 남아 있다(실행자로 옮기는 것은 별건). 실행자는 긴 트랜잭션을 들지 않는다 — 조회는 짧은 세션 하나, 이후 하트비트·트레이스·결과는 `TurnControl`이 호출마다 커밋한다. 이유: 트레이스 행이 flush만 되고 잡이 끝나야 커밋되면 다른 세션(이벤트 스트림)은 완료 때까지 아무것도 못 본다 — 인메모리 repo는 객체를 공유해 테스트에서 안 잡히는 종류라 실 Postgres 게이트 테스트(CI 서비스 컨테이너)로 고정했다. dispatch가 실패하면 턴을 `dispatch_failed`로 닫고 503(+쿼터 환불) — 안 그러면 커밋된 pending 행이 §5.4의 잠금이 된다.

### 5.2 취소

`cancel` 요청은 **턴 행**에 취소 플래그를 쓴다(초안은 "체크포인트에"였으나 실행 중인 스레드에 밖에서 쓰는 것은 체크포인트를 갈라놓는 바로 그 함정이라 novelty BR-RA8과 같은 행 플래그로 했다). 실행자는 **super-step 경계마다**(`decide` 진입) 하트비트를 찍으며 플래그를 읽고 멈춘다 — `heartbeat(owner_id, turn_id)` 한 문장(`UPDATE … RETURNING`)이고 owner 스코프다(INV-EV-1; 매 super-step 부르는 유일한 메서드가 격리를 못 지키면 안 된다) — 반복을 소모하지 않고 트레이스도 남기지 않는다. 진행 중인 `act`는 끝까지 돌고 그 결과도 부분 답에 들어간다(§2.8). 멈춘 시점의 상태로 `assemble`(PR 3부터 `answer → assemble`)을 돌려 부분 답을 만든다 — `stoppedReason=cancelled`, 근거 0건이면 `abstainReason=cancelled`(근거 부족과 구분). 이미 끝난 턴의 취소는 409. 실측: 취소 요청 후 28초에 부분 답(진행 중이던 추출이 끝난 뒤), 근거 8건·확인 2/16편.

### 5.3 진행

원천은 **Postgres 트레이스 행**이다 — API가 Fargate 2태스크(스티키 없음)라 프로세스 안의 큐·LangGraph 스트림으로는 재접속이 살지 않는다. `GET /turns/{id}/events?after=<seq>`는 접속 즉시 `accepted` 프레임을 보내고(수락 직후 침묵 금지), 1초 간격으로 새 트레이스 행을 `tool` 프레임으로 흘리다 턴이 종단이면 `result` 프레임(TurnOut) 후 닫는다. eventId는 `{turn}:{seq}`로 결정적이라 재접속에 같은 줄이 두 번 나오지 않는다. 조용한 구간은 15초마다 `: ping`(CloudFront 원본 유휴 30초 아래), 한 연결의 상한은 10분(넘으면 클라이언트가 `after`로 다시 붙는다). 싣는 것은 단계명·논문 제목·건수뿐이고 인용문은 싣지 않는다(C-2). `evidence.stream.first_token_ms`는 **첫 진행(tool) 프레임**까지의 지연이다 — `accepted`로 재면 동기라 구조적으로 0이고, 그러면 절대 회귀하지 않아 아무것도 못 잡는다. 폴링마다 짧은 세션을 연다 — 요청 스코프 세션을 스트림 내내 쥐면 풀 연결이 스트림 수만큼 묶인다. FE는 스트림 → `after` 재접속 3회 → `GET /turns/{id}` 2초 폴링 순으로 내려가고, 세션을 다시 열면 pending 턴에 같은 구독을 다시 붙인다.

### 5.4 세션 잠금

같은 세션에 턴이 겹치면 체크포인트가 갈라진다(Q5 조사 함정 2). 세션당 **진행 중 턴은 하나**로 제한한다 — 겹치면 409. 구현은 부분 유니크 인덱스(`evidence_turns(session_id) WHERE status='pending'`)라 두 API 태스크가 동시에 받아도 하나만 들어간다. 수락되지 않은 턴은 쿼터를 되돌린다. FE는 진행 중엔 입력을 막고 전송 자리에 취소 버튼을 둔다.

### 5.5 재실행 안전 · 고아 마감

취소·재개에서 도구가 다시 실행될 수 있다(함정 1). 검색·읽기는 부작용이 없다. **본문 승격 enqueue**만 부작용이 있으므로 paperId를 멱등 키로 쓴다(같은 논문 두 번 넣지 않음 — ingestion 큐 쪽 확인은 PR 4 재개와 함께).

**실행자가 죽은 턴**(배포·크래시): 하트비트가 `turn_stale_seconds`(기본 600 — 가장 긴 단일 단계인 승격 폴링 20초보다 충분히 크게) 넘게 끊긴 pending 턴을 폴링·이벤트·다음 수락이 발견하면 마지막 체크포인트 스냅샷으로 `assemble(INTERRUPTED)` → 부분 답(`stoppedReason=partial_failure`, 근거 0건이면 기존 기권 규칙). 조건부 UPDATE라 두 태스크가 동시에 발견해도 한 번만 쓰인다. 스냅샷이 없으면(체크포인터 없는 sqlite 개발·첫 스텝 전 죽음) `internal_error`로 닫는다. 종료 신호(SIGTERM·lifespan)는 다음 경계에서 `INTERRUPTED`로 닫고 나간다 — 실측 SIGTERM 2초 내 종료, SIGKILL 고아 턴은 재기동 후 첫 폴링에서 근거 4건의 부분 답으로 마감.

좁은 창 하나가 남는다: 재배달된 잡이 `run_loop` 진입에서 스레드를 비운 직후 죽으면 직전 스냅샷이 사라져 마감이 `internal_error`가 된다. 초안은 재개(PR 4)가 `delete_thread`를 걷어내면 함께 닫힌다고 적었으나, PR 4의 이어가기는 **이전 턴 스레드**를 읽는 방식이라 그 줄을 걷어내지 않는다(§3.1 참조). 이 창은 **그대로 남는다** — 닫으려면 재배달 방어를 대체할 것이 따로 필요하고, 그것은 이번 범위가 아니다.

---

## 6. 평가 — 골든셋·채점·지표

"제대로 작동한다"를 사람 느낌이 아니라 측정으로 판정한다. 틀은 업계 표준 하나다 — **문제집(골든셋) → 자동 채점 → 코드 바꿀 때마다 실행 → 실사용 지표 → 실패를 문제집에 추가.** 프로젝트마다 다른 것은 "무엇을 코드로 채점하고 무엇을 LLM 심판에 맡기느냐"뿐이다.

이 에이전트는 **"인용이 진짜인가"가 코드로 채점된다**(게이트). 보통 챗봇이 심판에 맡겨야 하는 가장 비싼 항목이 여기선 결정론이다. 그래서 무게를 코드 채점에 두고, 심판은 "판단 방향이 맞나" 하나에만 쓴다.

**시작 규모는 50문항**이다 — 유형별 균형, 각 문항에 정답 논문과 기대 판단 방향. 작성자는 사용자(논문을 아는 사람). 50이면 회귀는 잡히고, 실패 사례가 쌓이는 만큼 큰다(성숙 규모 200~500).

### 6.1 골든셋

| 유형 | 무엇을 묻나 | 정답에 들어가는 것 |
|---|---|---|
| 비교형 | "A가 B보다 좋아?" | 조건별 판단 · 갈림 지점 · 정답 논문 집합 |
| 주장형 | "X는 Y를 개선해?" | 지지/반박 판단 · 정답 논문 |
| 사실형 | "X는 언제·어디서?" | 값 · 출처 논문 |
| 후속 | 이전 턴 + 좁히기 | 좁혀진 논문 집합 |
| 코퍼스 밖 | 실시간 조회가 있어야 답할 수 있는 질문 | 정답 논문(코퍼스에 없음) |
| 범위 밖 | 잡담·코드·의견 | 검색 0회 · 안내문 |

유형별로 균형을 맞춘다. 실서비스 실패 사례는 골든셋으로 승격한다(실패 → 골든셋 루프).

### 6.2 3층 채점

| 층 | 무엇 | 어떻게 | 언제 |
|---|---|---|---|
| **1. 결정론** | 인용 실재율(=100%여야 함) · 게이트 탈락률 · 종합 문장 비율 · 반대 측 탐색 여부 · 범위 밖 질문의 검색 0회 · 정답 논문 recall@k | 코드 | **PR마다 CI** (LLM 응답은 녹화 픽스처) |
| **2. LLM 심판** | 판단이 정답과 같은 방향인가 · 조건 구분이 적절한가 · 갈림 지점이 맞는가 | 심판 모델, 채점 기준 명시. **심판 자체를 사람 라벨 표본과 일치율로 검증**한다 | 야간 / 수동 |
| **3. 사람** | 주간 표본 리뷰 | 사람 | 주간 |

검색 평가(recall@k)는 답변 평가와 **분리**한다 — 못 찾은 건지 찾고도 틀린 건지가 갈려야 고칠 곳이 보인다.

### 6.3 온라인 지표

트레이스(현행 `ToolCallRecord` + §4.3 검사 결과 + `question_kind`)에서 뽑는다: 기권율 · 취소율 · 턴당 근거 수 · 종합 비율 · 강등/거부/폴백 건수 · 실시간 조회 비율 · 첫 이벤트 지연 · 턴당 비용.

---

## 7. 운영 — 비용·트레이스

- **비용 권위는 U6 하나**(불변). 턴 예산 $0.50 유지, `answer`·재생성 비용 포함. 한도 도달 시 §2.9.
- **실시간 조회**는 소스별 서킷 브레이커 + 타임아웃. 한 소스가 죽어도 나머지로 진행하고, 셋 다 죽으면 코퍼스만으로 답하되 확인 범위 한 줄에 "실시간 조회 불가"를 싣는다.
- **트레이스**: 도구 호출 1:1 기록(현행) + `question_kind` + `stance` + 답변 검사 결과. 인용문·사용자 원문은 로그에 남기지 않는다(SEC-9).
- **체크포인트 보존**: 종단 턴의 스레드는 **7일**(`DOCSURI_EVIDENCE_CHECKPOINT_RETENTION_DAYS`) 뒤 정리 — 스케줄러 없이 턴 완료마다 상각해서 지우고, 세션 삭제·초기화는 그 세션 턴들의 스레드를 바로 지운다. 지운 턴에는 `checkpoints_pruned_at` 도장을 찍는다. 도장이 없으면 "오래된 종단 턴" 질의가 매번 같은 앞쪽 N건을 돌려줘 영원히 재삭제하고 그 뒤 턴은 영영 안 지워진다 — 로그의 `pruned: N`은 계속 정상으로 보인다.

---

## 8. 화면

| 요소 | 무엇 |
|---|---|
| **판단 산문** | 메시지 본문. `[n]`은 링크 — 누르면 근거 목록의 해당 명제로, 거기서 원문 블록으로(현행 앵커 이동). 대상이 접혀 있으면 목록이 함께 펴진다 |
| **종합 문장** | 본문 글자색을 한 단계 옅게 + 문장 끝 작은 "종합" 배지. 호버: "여러 근거를 종합한 문장이에요 — 원문에 그대로 있진 않아요" |
| **문장 역할** | 문장마다 한 단락. 결론은 한 단계 크고 굵게(**라벨은 없다** — 맨 위에 크게 오는 것으로 이미 결론이라고 말하고, 거기 "결론"까지 적으면 매 답변에 고정으로 붙는 절 머리표가 된다), 갈림 지점은 세로선으로 떼고 `갈리는 지점` 표시, 근거 서술은 표시 없음(대다수라 달면 소음이 된다). **표시 순서는 배열 순서다** — 역할로 다시 정렬하지 않는다(모델이 의도한 논지 전개가 어긋난다). §2.1의 "구획 라벨을 쓰지 않는다"가 이 자리를 정한다 — 매 답변에 고정으로 붙는 절 머리표(구 "핵심 주장"/"근거")를 금한 것이므로, **결론 라벨은 그것에 해당해서 빼고**(모든 답변에 있다) 갈림 지점만 남긴다(문헌이 갈릴 때만 나타나므로 붙어 있는 것 자체가 정보다) |
| **근거 목록** | **논문 하나가 블록 하나** — 제목 링크가 헤더에 한 번. 줄은 `[n]` + `✓`/`✗` + 앵커 링크 + 원문(표·그림·식은 명제). 논문마다 3건 이후는 접는다 |
| **쟁점** | 상충 명제만 목록 맨 위에 따로. 여기서만 줄마다 논문 이름이 나온다 |
| **근거로 쓴 논문 수** | 목록 아래 한 줄. 이 수는 `cited_paper_ids`이지 검색 범위가 아니다 — "검색 범위"로 적으면 코퍼스 전체를 돈 턴이 "1편만 봤다"로 읽힌다 |
| **확인 범위** | 잘렸을 때만 본문 끝 한 줄 (§2.3) |
| **진행** | 활동 피드(현행 구조) + **취소 버튼** |
| **없음·범위 밖·비용** | 전부 산문 메시지(§2.3·2.4·2.9). 현행 `abstainReasonLabel` 4종 문구는 폐기 |
| **모드 설명** | "질문을 던지면 여러 논문을 대조해 근거 카드로 정리해요" → **"질문을 던지면 논문 근거로 판단해 답해요"**. 힌트: "판단 · 근거 목록 · 논문 간 상충까지". 모드 이름은 **Evidence**다 — 백엔드·스키마·env가 전부 `evidence`인데 화면만 `Research`였다 |
| **진행 과정** | **한 줄로 접힌다.** 접힌 줄은 지금 단계와 누적 시간, 펴면 단계마다 이름·소요 시간·상세. 단계 이름은 도구 이름이다(`corpus_search` → 논문 검색 — 라벨 맵은 evidence 것이고, novelty는 같은 도구에 자기 문체의 라벨을 따로 갖는다). `accepted`는 단계가 아니라 첫 단계까지의 기준점. 상세에는 **결과 요약만** 싣는다 — 인자 요약은 모델 컨텍스트용 key=value 덤프라 실으면 그 절단 규칙이 사용자 계약이 된다. 사용자가 알아야 하는 검색어는 검색 도구가 자기 `query`로 결과 요약에 싣는다 |
| **타입** | `EvidenceResultPayload.answer` 구조 변경(§4.4) → `pnpm gen:types` 파급 |

---

## 9. 현행 대비 변경 지도

훑기에서 나온 11건이 어디서 해소되는지.

| # | 어긋남 | 해소 |
|---|---|---|
| ★1 | 판단 없음 | §4.2 `answer` 노드 |
| ★2 | 못 찾으면 끊김 | §2.3 산문 + §5.1 단일 경로 |
| ★3 | 취소 없음 | §5.2 |
| ★4 | 판단 검증 경계 | §4.3 검사 5종 + §4.4 계약 |
| 5 | 진입 경로 3갈래 | §5.1 하나로 |
| 6 | 비용 초과가 끊음 | §2.9 |
| 7 | 질문 유형 모름 | §3.3 `question_kind` |
| 8 | 충분 기준 = 근거 1건 | §3.3 바닥 2조건 |
| 9 | 검색 필터 없음 | §3.2 `year_from/to` |
| 10 | 다음 행동 자리 없음 | 버튼 없음으로 결정 — 산문으로 제안(§2.3) |
| 11 | 화면이 근거 카드를 광고 | §8 |

## 10. 실행 계획

| PR | 범위 | 완료 조건 |
|---|---|---|
| docs | 이 문서 | 머지 |
| **PR 1 옮기기** | §3.1 그래프로 이전. `answer` 없음, 바닥은 현행 규칙, 진입 경로 현행 유지 | 기존 루프 테스트 전부 통과 + 차등 검사 출력 동일 — **완료 2026-08-23(#30)** |
| **PR 2 실행** | §5 전부 — 단일 진입 경로·취소·세션 잠금·Postgres 체크포인트·고아 마감, FE 전환(취소 버튼·구독·재부착) | 실스택 눈확인(취소·409·SIGKILL·SIGTERM) — **완료 2026-08-24** |
| **PR 3 판단** | §4 `answer` 노드·검사 5종·계약 `segments`·§8 화면(산문+근거표·종합 배지·문구) | §6.2 1층 CI + 골든셋 초기분으로 2층 1회 — **완료 2026-08-24**(1층 위반 0 · 2층 17/18) |
| **PR 4 탐색** | §3.2 `live_lookup`·`stance`·`year` · §3.3 바닥 2조건 · §3.4 기억·이어가기(체크포인트 재개) · §6 골든셋 50 | 1층 CI에 반대 측 탐색·recall@k — **완료 2026-08-25**(2층 16문항 완주, 판정 41/45) |

**PR 4 실행 결과(2026-08-25, `feature/evidence-exploration`)**

- **연도 제약은 질의로 내려간다.** 상위 k를 받아 사후에 거르면 그 k 안에 해당 연도가 없을 때 0건이 나오고 그것이 "그런 논문이 없다"와 구분되지 않는다. `YearRange`를 U2 포트 셋까지 내려 OpenSearch `range` 절로 만든다. k-NN은 **ANN 자체의 `filter`**에 넣는다 — 사후 필터면 이미 고른 k를 자르는 것이라 좁은 창에서 요청한 top_k보다 훨씬 적게 돌아온다. `abstract_only`가 이미 그 자리를 쓰고 있어 덮지 않고 합쳤다(덮으면 lite scope가 조용히 사라진다). 목 어댑터도 실제로 거르게 했다 — 필터를 무시하는 대역은 필터가 동작한다고 보고한다.
- **`live_lookup` 세 소스.** 부분 저하는 결과값이고 **셋 다 죽었을 때만** 실패다. 처음 쓴 조건은 "결과 0건인데 죽은 소스가 있으면 실패"였는데 틀렸다 — 살아 있는 둘이 "그런 논문이 없다"고 답한 것이고 그것도 답이다(테스트가 잡았다). 죽은 소스는 모델 관찰과 화면 확인 범위 줄 양쪽에 알린다(소스 이름은 안 싣는다 — SEC-9). OpenAlex 초록은 역색인이라 되펼친다: 안 하면 초록이 통째로 비고 초록만 확보하는 도구에서 그것은 결과가 없는 것과 같다. arXiv에 없는 논문은 `doi:` 네임스페이스로 나르고 arxiv.org id를 지어내지 않는다.
- **개명이 조용히 깨뜨리는 자리 셋.** 캡 키가 곧 도구 이름이라(`max_tool_calls.get(tool_name)`) 안 옮기면 캡이 무한대가 되고 예외도 로그도 없다. 1층의 검색 도구 집합이 어긋나면 `searches`가 0이 되어 "범위 밖 질문은 검색 0회" 검사가 항상 통과한다. 모델에게 도구 이름을 문장으로 지시하던 안내문 2곳은 테스트도 런타임도 안 잡고 품질만 떨어뜨린다. `PaperOrigin.EXTERNAL`은 도구 이름이 아니라 "승격이 필요한 논문"의 뜻이고 체크포인트 직렬화 값이라 그대로 뒀다.
- **`external_search`는 배포에서 한 번도 돈 적이 없었다.** 게이트 플래그가 `.env.example`에도 CDK에도 없었고 그 사실이 어디에도 안 적혀 있었다. `live_lookup` 플래그·자격증명을 양쪽에 배선하고 기본값을 off로 뒀다(자격증명 이름은 ingestion 것을 그대로 쓴다 — 같은 키를 두 이름으로 두면 언젠가 한쪽만 채워진 배포가 나온다).
- **바닥 2는 "찾았는가"가 아니라 "찾아봤는가"다.** 0건·도구 실패는 세고, 예산 거부·없는 도구 호출은 안 센다(도구가 돌지 않았으므로 세면 선언만 붙여 부르는 공짜 통로가 된다). 찾았을 때만 인정하면 반대 근거가 실재하지 않는 질문과 인덱스가 죽은 턴이 영원히 못 끝난다. `stance`는 `ToolCallRecord`의 일급 필드다 — `args_summary`를 되파싱하면 판정이 렌더 형식에 묶여, 요약 길이를 줄이는 무해한 변경 하나가 반대 측 탐색을 0회로 만든다.
- **`_PRIOR_TURNS = 3` 폐기.** 짧은 턴 셋과 긴 턴 셋이 같은 3인데 실제로 드는 비용은 열 배 갈렸다. 토큰 예산(8천)까지 최근 턴을 그대로 싣고 넘치는 앞쪽을 요약으로 접는다 — 반대로 하면 방금 한 질문이 뭉개져 후속 질문 해석이 가장 필요한 자리에서 정보가 가장 적어진다. 접을 때 모델을 부르지 않는다(질문 목록이 곧 요약이고, 부르면 턴 실행 전에 왕복이 하나 더 생겨 그 비용이 턴 예산 밖에서 나간다).
- **골든셋 13 → 51.** 배포 색인에서 초록을 직접 읽고 골랐고 순위로 고르지 않았다(순위로 고르면 recall@k가 자동으로 1.0이 되어 아무것도 못 잰다). 새 라벨 10건은 **2026-08-25 검수 완료**라 채점 표본은 6 → **16문항**이 됐다. **검수 게이트는 빨간 빌드에서 구조로 옮겼다** — 종전 규칙은 문항을 늘릴 때마다 CI를 빨갛게 만들 뿐 미검수 라벨로 점수를 재는 것 자체는 막지 못했다(빨간 채로 점수는 그대로 나왔다). 지금은 채점 경로가 미검수를 안 받는다. 그 불변식은 **합성 사례로** 검사한다 — 검수 대기가 빈 날에도 규칙이 살아 있어야 하고, 실제 데이터로 검사하면 그날 아무것도 안 보면서 초록으로 남는다.
- **2층의 1 fail은 절반이 하네스였다.** 심판이 후속 문항에 `prior_topic` 문자열만 넘기고 이전 턴을 실제로 돌리지 않았다 — 인용 논문도 이어가기 씨앗도 없이 도는 것이라 좁히기·이어가기가 구현돼도 거기서는 영영 실패한다. 이제 이전 턴을 진짜로 돌리고 체크포인터를 붙여 조립한다.
- **1층 실측(녹화 픽스처)**: 51문항 · 위반 0 · 인용 실재율 1.00 · 반대측 탐색 비율 1.00 · recall@k 1.00 · 종합 문장 비율 0.50 · 폴백 0.00 · 재생성 0.00 · 강등 0. **이 수치를 품질로 읽으면 안 된다** — 1층은 LLM을 타지 않고 대본이 정확히 기대대로 움직이므로 recall@k·반대측 비율이 구조적으로 1.00이다. 여기서 재는 것은 **배선과 불변식**이고, 값이 1.00에서 떨어지면 품질이 아니라 배관이 깨진 것이다(§6.2). 품질은 2층과 실서비스 지표가 본다. 레인: shared 113 · discovery 159 · backend 763 · frontend 331 통과. 커밋 5개 각각 단독 통과(bisect).
- **실스택 확인(2026-08-25, 로컬 스택 + 실제 외부 API)** — 목으로는 못 잡는 것 둘을 잡았다.
  - **연도 필터**: 실색인(`docsuri-deploy-v1`)에 `range` 절이 그대로 내려가고 bm25·phrase·k-NN 셋 다 동작한다. `year_from=2024`가 30편을 **가득** 채워 돌려주므로 사후 필터가 아니라 재검색임이 확인된다. 무제한과 연도제한에서 **같은 논문의 BM25 점수가 소수점까지 동일**해 `filter` 절이 점수에 안 닿는 것도 증명됐다. `abstract_only`와 연도를 함께 걸어도 둘 다 유지된다. 도구 층까지 이어 확인(문자열 연도·0건 사유 노트 포함).
  - **live_lookup**: arXiv 8건·OpenAlex 8건 정상. OpenAlex **역색인 초록 복원이 실응답에서 동작**한다(빈 초록 0/8) — 이게 안 되면 초록만 확보하는 도구에서 결과가 없는 것과 같다. 중복 제거 후 16편, `doi:` 논문이 승격 큐로 안 간다.
  - **S2는 무키로 사실상 항상 429다**(12초 간격 5회 연속, 요청 모양은 정상). 그런데도 매 턴 부르면 왕복을 버리고 화면에 "실시간 조회가 온전히 돌지 못했어요"가 **상시로** 뜬다 — 미구성을 장애로 보여주면 진짜 장애가 묻힌다. **키가 있을 때만 합류**하도록 바꿨고(도구 등록의 자연 축소와 같은 규칙), 실패 판정도 "셋"이 아니라 **구성된 소스 전부**가 기준이다. 키가 없는 배포에서 live_lookup은 arXiv·OpenAlex 둘로 돈다. **S2 파서는 실응답으로 검증되지 않았다** — 키가 생기면 그때 확인한다.
  - 같이 잡은 것: `live_lookup`이 arXiv에 없는 논문을 `doi:`로 실어 오는데 `fetch_paper`가 그것을 승격 큐로 보내고 있었다. U1은 arXiv만 빌드하므로 못 만드는 잡이 들어가 20초 폴링을 태우고 `timed_out`으로 끝난다 — 결과는 "초록 범위로 계속"으로 같아 **로그만 봐서는 정상**이다.
- **정리 리뷰(2026-08-25)가 잡은 것 — "지금 사실이 아닌 것" 넷.**
  - **세션 요약이 매 턴 덧붙고 있었다**(실측 1→2→3회). §3.4가 "매 턴 재요약하지 않는다"고
    정했는데 접혔다는 사실이 어디에도 안 남아 같은 창을 다시 접었다. 요약 상한이 **앞을
    자르므로** 사본이 쌓이면 정작 보존하려던 옛 내용이 밀려 나간다 — 로그로는 "요약이 커지고
    있다"로 보인다. `checkpoints_pruned_at`과 같은 도장(`006` 마이그레이션)으로 닫았다.
    **내 테스트가 그것을 통과시켰다**(`count(...) >= 1`) — 검사가 불변식이 아니라 "뭔가
    있다"를 보면 그 검사는 없는 것과 같다.
  - **요약 경로가 애초에 도달 불가였다.** 예산을 질문 문자열로만 재서(턴당 15~25토큰) 8천
    토큰이 영원히 안 물었다. 계량을 턴 페이로드로 바꿨지만 그것만으로도 부족했다 — 현실적인
    턴은 144자라 예산이 222턴에서야 찬다. 진짜 사라지던 것은 **천장(20턴) 밖으로 밀려 애초에
    안 읽히던 턴**이었다. 결과 JSON을 역직렬화하지 않는 좁은 질의를 더해 그쪽을 접는다.
    실측: 22턴째부터 하나씩 접히고, 30턴 세션에서 가장 오래된 질문이 요약에 남는다.
  - **arXiv id 정규식 두 벌이 이미 갈려 있었다** — 한쪽만 대소문자를 받아 `HEP-TH/9901001`이
    한쪽에서만 인식됐다. 그 논문은 `doi:`로 실려 **승격도 색인도 안 되고** 사용자에게는
    "이 논문은 arXiv에 없다"는 확신에 찬 오답이 나갔다. 문법 조각 하나를 공유한다.
  - **씨앗이 틀린 버킷으로 갔다.** 이전 턴 논문을 확인분(`papers`)에 심어 `examined`가
    부풀었다 — 화면의 "후보 N편 중 M편 확인"이 이번 턴이 열어보지도 않은 논문을 셌다.
    §3.4에 내가 쓴 문장("관찰의 **확인 대기 논문**에 실어")과 코드가 달랐다.
  - 함께 걷어낸 것: 도달 불가한 제목 사다리와 그것만 읽던 필드들, 호출자가 0인 `graph=` 인자,
    `getattr`로 포트를 우회하던 분기(계약을 넓혀 없앴다), 세 곳에 있던 같은 상수, 클래스
    속성을 갈아끼우던 테스트 3곳, 공용으로 이미 뽑혀 있던 헬퍼의 네 번째 사본.
  - **코드 리뷰가 더 잡은 것**: 실시간 조회가 **통째로** 죽은 턴이 확인 범위 줄에서 침묵하고
    있었다 — 부분 저하는 표시하는데 전면 실패는 플래그를 안 세웠다. 정확히 거꾸로다(계약
    설명은 "셋 다 죽은 턴도 true"라고 적혀 있었다). 그리고 정리 패스에서 검색 0건 노트를
    고쳤다고 적었는데 **문자열이 안 맞아 수정이 조용히 안 먹었다** — 매개변수만 생기고
    덮어쓰기는 그대로였다. 회귀 테스트를 걸어 닫았다.
  - 렌더 상한(8)과 접히는 경계(20)가 달라 **9~20번째 턴이 어디에도 안 실리고 있었다** —
    접히지도 않고 보이지도 않았다. 경계를 하나로 만들었다.
  - 요약 머리표는 저장 쪽이 소유한다 — 두 모듈이 같은 문자열을 각자 쓰면 한 글자만 달라져도
    조용히 안 맞고 라벨이 다시 쌓인다.
  - **코드 리뷰 2차가 더 잡은 것 — 여섯 건, 전부 재현했다.**
    - **중복 제거 사다리가 DOI를 앞에 둬 같은 논문이 안 접혔다.** arXiv 소스는 DOI를 아예 안
      싣고 S2/OpenAlex는 출판된 논문에 대개 싣는다 — 실측으로 한 논문이 후보 **세 건**으로
      남았다. 그 뒤는 조용히 나쁘다: 모델이 한 논문을 세 출처로 보고, 승격이 두 번 돌고(각
      20초 폴링), 색인 잡이 두 철자로 나가고, 같은 논문이 독립 근거로 두 번 인용될 수 있다.
      **arXiv id를 먼저 본다.** ingestion이 DOI를 앞에 두는 것은 수확 경로에 arXiv 전용
      레코드가 없기 때문이고, 여기는 그 전제가 다르다. arXiv DataCite DOI(`10.48550/arXiv.…`)
      에서도 id를 되찾는다.
    - **뒤 소스가 구조적으로 안 보였다.** 소스마다 8건인데 도구가 상위 10건으로 자르니
      arXiv 8 + S2 2로 차고 OpenAlex는 한 번도 안 보인다 — S2·OpenAlex를 더한 이유가
      "arXiv에 없는 논문"인데 그 논문이 잘려 나갔다. 소스별로 번갈아 담는다.
    - **첨부한 사적 문서가 다음 턴으로 샜다.** 첨부 핸들은 `abstract_text`에 사용자가 올린
      본문을 들고 스냅샷에 실리는데, 씨앗이 그것을 걸러내지 않았다 — 소유권·범위 재확인
      없이 인용될 수 있고 업로드를 지운 뒤에도 살아남는다. `_seed_explicit`가 막는 것과 같은
      우회가 씨앗 경로로 돌아 들어왔다.
    - **씨앗이 세션 내내 불어났다**(실측 5 → 10 → 15). 씨앗이 새 턴의 후보가 되고 그 후보가
      다시 스냅샷에 실린다 — 그 수가 화면의 "관련 논문 N편 중 M편 확인"의 N이라, 한 번
      검색한 턴이 "300편 중 4편"이 된다. 상한을 뒀다.
    - **바닥에 공짜 통로가 있었다.** `extract_evidence(paper_ids=[])`처럼 **포트에 닿기도 전에
      인자 검증에서 떨어지는 호출**이 반대 측 탐색으로 세어졌다(실측). 실패를 센 이유는
      "인덱스가 죽은 턴이 영원히 못 끝난다"였는데, 그 걱정은 세는 규칙이 아니라 **바닥 쪽
      탈출구**로 막는 것이 맞다 — stance 도구를 더 부를 예산이 없으면 종료를 받아들인다.
    - 그 탈출구가 없어서, 예산이 소진된 주장형 턴은 종료 제안이 매번 거부되며 남은 반복을
      LLM 호출로 태우고 `budget_exhausted`로 끝났다 — 깨끗이 끝냈어야 할 턴이 화면에
      "이어서 확인할까요?"를 띄웠다.
    - 인메모리 저장소만 삭제된 세션의 요약 쓰기를 받아 줘 SQL과 갈렸다(테스트가 보는 쪽이
      인메모리라 그 갈림이 초록으로 남는다).
  - **남긴 것**: `SourceBreaker`가 429를 곧바로 재시도하는 것(왕복 두 번)은 novelty 모듈의
    계약(NFR-NV2-11)이라 이 PR 밖이다. 주 원인이던 무키 S2는 이미 제외했다.
  - 낭비: 세 소스를 **동시에** 던진다(실측 — 두 소스가 0.04초·0.05초에 함께 시작한다.
    순차였다면 뒤엣것은 15.68초 뒤에 시작했다). 색인은 배치 한 번으로 보낸다.

- **2층 1회 실행 — 16문항 완주, 판정 41/45 pass (2026-08-25 재실행).** 처음 두 번은 **정확히
  3문항 뒤** 죽고 회복하지 못했다(문항 간격 8초·45초 양쪽, 같은 4번째 문항). 원인은 쿼터가
  붐빈 것이 아니라 **구조**였다 — 셋이 겹친다: 조립이 botocore 재시도를 일부러 끄고
  (`max_attempts: 1`), `SourceBreaker`의 재시도가 **즉시 1회**라 요청속도 스로틀에 무용하며,
  차단기 쿨다운(60초)이 심판 간격(45초)보다 길어 **다음 문항이 열린 회로로 걸어 들어갔다.**
  4번째가 하필 후속형이라 이전 턴까지 두 턴을 연달아 돌아 호출이 두 배인 자리였다.
  → 요청속도 거절일 때만 지터를 실어 기다리도록 고치고(계약이 정한 것은 횟수이지 간격이
  아니다), 심판 간격 기본값을 쿨다운보다 크게 올렸다. **재실행에서 스로틀로 죽은 문항 0건.**
- **수치**: 인용 실재율 1.00 · 폴백 0.00 · 재생성 0.00 · **recall@k 0.556** · 반대 측 탐색
  비율 0.833 · 위반 2건(예산 소진·취소로 끝나 바닥 검사를 지나지 않은 턴). **recall@k가
  처음으로 진짜 값이다** — 1층의 1.00은 녹화 대본이 만든 구조적 값이고, 이것이 3,248편
  코퍼스가 실제로 답할 수 있는 범위다.
- **심판이 잡은 fail 4건 중 셋이 한 문항이다.** `smoothquant_vs_qat`가 갈림 지점을 다른 축으로
  잡았다(기대는 "재학습 비용을 감당할 수 있는가", 답은 "PTQ의 종류"). `self_rag_improves_over_plain_rag`은
  방향·조건을 맞히고 원인 설명만 어긋났다. 둘 다 트레이스는 건강하다(도구 12~13회·오류 0·
  강등 0~1) — **검색이 아니라 판단**이다.
- **별건으로 남는 것**: `dropped parallel calls`가 16문항 **전부**에서 2~11회씩 났다. 모델이
  도구를 여러 개 한 번에 부르는데 루프가 하나만 받고 나머지를 버린다. 결과를 오염시키지는
  않지만(호출이 죽은 것이 아니라 안 보낸 것) **모델이 만든 계획의 절반을 버리면서 그 토큰값은
  이미 냈다.**
  - **돈 3건은 판정 9개(방향·조건·갈림 지점) 전부 pass.** `rag_reduces_hallucination`(recall@k 0.33 · counter 1회) · `lora_vs_full_finetuning`(0.33 · counter 3회) · `cot_prompting_definition`(1.00 · 사실형이라 counter 0회로 면제). 주장·비교형 둘 다 반대 측 탐색이 실제로 잡혔고, 심판이 갈림 지점을 "노이즈 비율 80% 초과"·"사전학습 분포 내 표준 태스크냐 vs 장문 컨텍스트 확장이냐"로 짚었다고 판정했다. **n=3은 품질 판정이 아니다** — 배선이 돈다는 것까지가 이 값의 값어치다.
  - **하네스가 오염을 조용히 넘기지 않았다.** `throttled_or_llm_down`에 13건이 이름째 실리고 "이 실행의 수치는 그만큼 신뢰할 수 없다"고 경고했다. 이것이 없었으면 `cases: 3`을 보고 "3문항 전부 통과"로 읽었을 것이다 — 요약이 **분모를 줄여서** 좋아 보이는 모양은 PR 3의 평균 필터 결함과 같은 종류다.
  - `reports/evidence-judge-2026-08-25.json`에 전 문항의 상태·트레이스·오염 로그가 남아 있다.

처음엔 "PR 2가 크지만 나누지 않는다 — 같은 계약을 공유한다"고 적었으나 틀린 근거였다. §5(엔드포인트)·§4(답변 페이로드)·§3(도구)는 서로 다른 계약이라 각각 단독으로 동작하고, 한 PR로 묶으면 리뷰도 bisect도 되지 않는다. 2026-08-23에 셋으로 나눴다.
