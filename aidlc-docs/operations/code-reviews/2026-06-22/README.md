# Code review & traceability snapshot — 2026-06-22

Generated by the adopted AI-DLC companion tools (`tools/aidlc-traceability` and the upstream
`aidlc-codereview`). Regenerable artifacts, kept for reference.

- `codereview-backend-summary.html` — `aidlc-codereview` entry point (open this first).
- `codereview-backend-technical.md` — full technical report on `backend/`: **1182 findings**
  (269 MEDIUM / 125 LOW / 788 INFO) + **15 AI-flagged critical sections**. Candidates for human review, not confirmed bugs.
- `traceability-matrix.md` — `tools/aidlc-traceability` output: **936 relationships / 917 edges**.

## Regenerate

```bash
# traceability (rule-based, free)
uv run --directory tools/aidlc-traceability traceability generate --input . --no-ai --format markdown -o <out>

# codereview (needs Amazon Bedrock; run from the upstream aidlc-codereview checkout)
AWS_PROFILE=<docsuri-profile> AWS_REGION=us-east-1 aidlc-code-reviewer backend/ --technical-report -o <out>
```
